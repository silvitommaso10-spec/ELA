Fase: v0.1 — fake in-memory di ogni port per i test delle milestone successive (spec §51, ADR 0005). Mai importato da codice di produzione. Nessun fake per `AuthorizingGuardianPort` (ADR 0013 §4): il Guardian vero è puro e gira sull'`AuditLog` fake. `FakeVerifier` (ADR 0014) risponde da una tabella condizione → fallimento e onora il contratto di `check_verifiable` come i verifier veri.
