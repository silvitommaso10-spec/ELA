Fase: post-0.1 — Work Node macOS (spec §5).

Implementato in M12.3, e il codice vive in `src/ela/node/` — non qui (ADR 0039 §1). Si avvia con
`uv run ela node run`, in primo piano, in un terzo terminale accanto a `ela serve`.
