Documentazione del progetto: [`GETTING_STARTED.md`](GETTING_STARTED.md) (far partire ELA e farle
fare la prima cosa, comando per comando), `spec/` (specifica, fonte di verità), `adr/`
(Architecture Decision Records), `milestones/` (spec di ogni milestone), `examples/` (i due piani
di esempio: quello che `GETTING_STARTED` usa e uno step `model.complete` per quando il provider
avrà una chiave — un test li manda all'API così come sono, perché non marciscano).
