Documentazione del progetto: [`GETTING_STARTED.md`](GETTING_STARTED.md) (far partire ELA e farle
fare la prima cosa, comando per comando), `spec/` (specifica, fonte di verità), `adr/`
(Architecture Decision Records), `milestones/` (spec di ogni milestone), `examples/` (il piano di
esempio che `GETTING_STARTED` usa; un test lo manda all'API così com'è, perché non marcisca).
