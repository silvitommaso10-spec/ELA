# M12.5 — Il companion iPhone: vedere e rispondere da lontano, e un campanello che non porta lettere

- **Stato:** **Proposta** — stesura del 2026-09-19, **da rivedere**: nessun codice. La ricognizione
  è fatta dal Mac, sul codice, sui documenti e sulla documentazione pubblica di Apple, ntfy, Pushover
  e Tailscale; ciò che solo l'iPhone può dire è nelle misure P0–P4 (§«Che cosa solo l'iPhone può
  dire», con i passi in appendice), che Tommaso esegue a mano prima che si chiudano le domande che ne
  dipendono. **Già riletta una volta** da un agente separato: i rilievi sono verificati sul codice e
  corretti qui (§«La rilettura, e che cosa ha cambiato»). Le domande sono numerate in §«Da portare
  all'utente».
- **Riferimenti spec:** §6 (**iPhone — Companion Node**), §8 (ELA non deve essere sempre rumorosa),
  §15 («essere monitorata da iPhone»), §16 (Device Registry), §27 (Guardian), §29 (livelli di
  rischio), §30 (un «Sì» fuori contesto), §32 (Audit Log), §33 (fail-safe), §57 (privacy), §58
  (sicurezza), §62 (chiedere), §65 (governabile).
- **Riferimenti design:** `docs/spec/ELA_design.md` — §5 del design (la presenza), §6 del design (gli
  stati), §11 del design (il Device Center, la voce iPhone), §14 del design (Approval Center), §20
  del design (**iPhone**), §29 del design (design e sicurezza); voce 17 di §34 del design.
- **Branch:** `m12.5-iphone-companion`, da `main` dopo il merge di `docs-conteggi-derivati`
  (`7556959`).
- **ADR previsto:** **0043**. Tre cose che questa milestone decide sopravvivono a lei: **che cos'è
  un'identità che non prende lavoro** — un ruolo imposto all'arruolamento, accanto a `privacy`, che
  ADR 0037 §4 ha lasciato «alla milestone che lo usa»; **la forma del client web** — chi serve le
  pagine, come portano l'identità, se c'è JavaScript nostro —, che M17.2 eredita (punto fermo 1, e
  la voce 5.10 di `docs/STATO.md` rivista apertamente in dec. D); e **il campanello** — un
  vocabolario chiuso che attraversa un server di terzi, derivato e appuntato come `WireCode`.
- **Dipende da:** M12.1 (ADR 0037), M12.2 (ADR 0038), M12.3 (ADR 0039), M12.4 (ADR 0040), M17.1
  (ADR 0042).
- **Eredita:** M12.1 **D1–D20**, M12.2 **dec. A–P**, M12.3 **dec. A–M**, M12.4 **dec. A–K**, nel
  testo autorevole dei loro documenti. Qui pesano: D3 (la metà imposta la scrive l'utente), D4 (il
  Core non inizia mai verso il nodo — «vale … per l'iPhone», `M12.1.md:256`), D5 (un segreto per
  identità, mai in un URL), D10 (un middleware, nessuna rotta fuori), D12 (chi agisce è l'identità
  risolta), D18 (la sensibilità del task contro il tetto del nodo), D19 (nessun filtro prima dei
  rifiuti).
- **Continua:** ADR 0015 §1 (`pending`, «per M8.1 e per l'iPhone»); ADR 0023 §4 (`responded_by`
  diventa un'identità autenticata), §7 (un browser non manda `Authorization`); ADR 0024 §2 (la CLI è
  un client, non un secondo ELA); ADR 0026 §7 (un rifiuto che non può scattare); ADR 0035 §7 (un
  test che si accorge del proprio pagamento); ADR 0037 §2, §3, §4, §5, §6, §7, §12, §13, §15; ADR
  0038 §16, §18; ADR 0042 §1, §3, «Conseguenze».
- **L'ultima della Fase 12.** La chiude: ogni rimando del repository alla Fase 12 e a M12.5 è in
  dec. G, onorato qui oppure spostato con la sua casa nuova nominata.

> Le decisioni di questa milestone hanno **lettere**. Le lettere dalla A alla G sono le domande
> dell'utente, **nello stesso ordine e con lo stesso significato**; H e I sono nate dalla
> ricognizione. Quelle ereditate portano il prefisso della milestone che le registra («M12.1, D5»,
> «M12.4, dec. F»). I sei **punti fermi** dell'utente si citano come «punto fermo *n*».

## Obiettivo

> **Lontano dal Mac, l'utente sa che ELA lo aspetta, vede che cosa gli chiede e che cosa sta
> facendo, e risponde. Il campanello che lo chiama non porta niente di suo; la pagina che apre sta
> sulla tailnet e ha l'identità di M17.1; e l'iPhone che risponde è un'identità che l'utente ha
> ammesso, che firma ciò che sceglie e che smette di poterlo fare quando l'utente la revoca.**

**Criterio di fine (proposto):** *con il Core su questo Mac e l'iPhone sulla tailnet, un task
dichiarato `TRUSTED` con uno step che chiede consenso fa suonare l'iPhone, e il testo della
notifica è una voce del vocabolario chiuso; il tocco apre in Safari la pagina di ELA, che nomina ciò
che si approva; il «sì» dato lì scrive l'id dell'iPhone in `responded_by` e nell'attore di
`APPROVAL_RESOLVED`, e il task prosegue su questo Mac (dec. H); lo Shortcut dice quante cose aspettano; revocato,
l'iPhone riceve 401 alla richiesta successiva e la pagina torna a chiedere un codice. Niente di
tutto questo chiede una chiave di Anthropic.*

**Perché senza la chiave è fattibile, verificato e non assunto.** Un piano si scrive a mano (`ela
task plan --file`, `cli/tasks.py:150-170`), e due capability di produzione chiedono consenso senza
parlare con un modello: `voice.speak` è `MEDIUM` con `requires_authorization`
(`permissions/capabilities.py:497-512`) e gira con `say` sul Mac; `core.echo` è `SAFE`, e chiede
consenso quando lo step porta `requires_authorization: true` (`api/schemas.py:156`,
`permissions/guardian.py:318-323`). Il campanello su ntfy.sh non vuole un account (dec. E). La prova
a mano (§«La prova a mano») usa le due.

## Che cosa esiste già

Il censimento del codice, a `7556959`. Ciò che conta per il companion:

| Tema | Oggi | Dove |
|---|---|---|
| **Le identità** | tre, da un header solo: il Core (`Bearer <ELA_API_TOKEN>`), un nodo (`Bearer <device_id>.<segreto>`), un codice di arruolamento. `Identity.actor` è `LOCAL_USER` per il Core e `Actor(DEVICE, id)` per un nodo | `api/security.py:90-135` |
| **Le rotte di un nodo** | `NODE_ROUTES`, coppie letterali (metodo, percorso); il codice solo su `POST /nodes/enroll`. Un nodo **non** raggiunge `/approvals`, `/tasks`, `/devices`, `/audit` | `api/security.py:65-84` |
| **Chi risponde** | `responded_by=identity.actor.id`, dall'identità risolta e mai dal corpo; oggi sempre `str(LOCAL_DEVICE_ID)`. La regola 19: `.respond(` ha un solo chiamante, `api/approvals.py` | `api/approvals.py:78-83` |
| **Rispondere non fa ripartire** | `approve` porta il task a `QUEUED`; **solo** `POST /tasks/{id}/run` lo esegue | `tasks/engine.py:624-653`, `api/tasks.py:107-127` |
| **Che cosa porta un'approvazione** | `ApprovalOut`: `id`, `created_at`, `task_id`, `step_id`, `capability_id`, `targets`, `prompt`, `status`, `expires_at`, `responded_at`, `responded_by`. **Né il rischio, né la sensibilità del task, né i termini del grant** | `api/schemas.py:309-338` |
| **La domanda** | `f"{spec.id}{where} for step {step.id} ({step.goal}){stated}{_may_travel(task, stated)}: {reason}"` — porta lo scopo dello step (contenuto dell'utente) e solo gli argomenti di `prompt_arguments` (`purpose`, `seconds`) | `executive/executor.py:1368-1369` |
| **Il grant di un «sì»** | un uso; lo conia l'executor al `run` che segue il «sì», e scade `ELA_AUTHORIZATION_TTL_SECONDS` dopo quel `run` (default 3600, tetto 24 h); la domanda scade dopo `ELA_APPROVAL_TTL_SECONDS` (86400) | `permissions/authorizations.py:155-192`, `composition/settings.py` |
| **Quali domande possono nascere** | `RISK_POLICY`: `HIGH` e `CRITICAL` sono `DENY`. Una domanda oggi nasce solo per `MEDIUM`, o per `SAFE`/`LOW` con `requires_authorization` | `permissions/guardian.py:118-126` |
| **Fermare un task (§65)** | `POST /tasks/{id}/cancel` c'è, firmato dall'identità risolta; lo raggiunge solo il Core | `api/tasks.py:138-146` |
| **I nodi in `GET /devices`** | `available` = `available()`: heartbeat entro il TTL (60 s) e non revocato. `OperatingSystem.IOS` esiste | `devices/registry.py:128-135`, `:237-256`; `domain.py:557` |
| **Pagine** | niente in `src/` serve HTML o file statici; il middleware dà 401 a ogni richiesta senza `Bearer`, percorsi inesistenti compresi. La documentazione HTML è spenta perché «un browser non manda un header `Authorization`» (ADR 0023 §7) | `api/app.py:221-227` |
| **Notifiche** | nessuna, nessun port | — |
| **La suite di conformità** | tredici storie (`STORIES`), tre kit (`FAKE`, `MACOS`, `WINDOWS`), e il docstring «M12.5 appends its own here» | `tests/conformance/driver.py:39-53`, `test_node_contract.py:67-69` |
| **Ogni rotta ha un comando** | salvo le rotte che chiama un nodo, classificate per nome (`NODE_CALLED`) | `tests/docs/test_adr_cli.py:248-253` |

## La ricognizione

### Che cosa ho potuto stabilire da questo Mac

Documentazione pubblica, letta il 2026-09-19. **Niente di questo è una misura**: è ciò che una
misura deve confermare o smentire.

**iOS 27 è uscito**: la guida di Comandi è «ios/27», e WebKit ha pubblicato «WebKit Features for
Safari 27.0» il 2026-09-17. La versione sull'iPhone la dice P0. «Documentato» vuol dire una pagina di
Apple, di WebKit, di ntfy, di Pushover o di Tailscale; «dal sorgente» vuol dire il repository del
produttore al 2026-09-19, che è una prova primaria ma può differire dal binario installato;
«secondario» vuol dire un riferimento di terzi.

| Tema | Che cosa risulta | Fonte | Che cosa manca, e dove si prende |
|---|---|---|---|
| **Comandi: la richiesta** | «Ottieni contenuto dell'URL» fa `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, con un corpo JSON, un modulo o un file. Il campo delle intestazioni c'è, ma lo documenta solo un riferimento di terzi | support.apple.com/guide/shortcuts/apd58d46713f (iOS 27); secondario per le intestazioni | se `http://` verso un indirizzo IP passa — per le app di terzi, «In iOS 17 … ATS no longer allows connections to IP addresses by default» (developer.apple.com, `NSAllowsLocalNetworking`), e la configurazione di Comandi non è pubblica: **P2** |
| **Comandi: il tempo** | nessun timeout documentato; una sola voce di forum parla di ~25 s | secondario | nessuna attesa lunga: C2 lo esclude comunque |
| **Comandi: le intestazioni della risposta** | «Ottieni intestazioni dell'URL» fa una `HEAD` a parte; le intestazioni di un `GET` o di un `POST` non si leggono | secondario | per questo il companion non usa `ETag` né `If-Match` (C2) |
| **Comandi: le automazioni** | «Ora del giorno» si ripete ogni giorno, settimana o mese; gli altri inneschi sono eventi. **Nessun intervallo di minuti** | support.apple.com/guide/shortcuts/apd932ff833f (iOS 26, 27) | niente: un heartbeat dallo Shortcut non esiste |
| **Comandi: iCloud** | la sincronizzazione è un interruttore (Impostazioni → Comandi rapidi → Sincronizzazione iCloud) e porta gli Shortcut su tutti i dispositivi. Con la **Protezione dati standard** le chiavi le tiene Apple; solo con la Protezione dati avanzata sono cifrati da capo a capo | support.apple.com/guide/shortcuts/apdb3a4240b0; support.apple.com/102651 | se il testo di una prova arriva sul Mac: **P2** |
| **Comandi: condividere** | un campo con una «domanda di importazione» si svuota quando lo Shortcut si condivide; **gli altri campi viaggiano come sono scritti**. Il link sta su icloud.com; condividendo il file con «Chiunque», «Apple will receive a copy of your shortcut for validation» | support.apple.com/guide/shortcuts/apdf330fd3a0, apdf01f8c054 | niente: è la ragione di C.2 |
| **Comandi: un portachiavi** | nessuna azione documentata | — | niente |
| **Comandi: i file** | «Salva file» e «Ottieni file» li documenta solo un riferimento di terzi | secondario | se un file «Sul mio iPhone» si salva e si rilegge: **P2** |
| **Safari: `Secure` su `http`** | Safari 27 permette i cookie `Secure` su `http` **solo per il loopback**: su un `100.x` no | webkit.org/blog/18325 | niente: il cookie nasce senza `Secure` |
| **Safari: `SameSite` di default** | Safari 18.0 ha **tolto** il trattamento di un cookie senza `SameSite` come `Lax`: il default non protegge | webkit.org/blog/15865 | niente: `SameSite=Strict` si scrive sempre |
| **Safari: `Origin`** | un `POST` di un modulo porta l'header `Origin` (WebKit, bug 20792, risolto nel 2008) | bugs.webkit.org/show_bug.cgi?id=20792 | che arrivi davvero dalla tailnet: **P1** |
| **Safari: i cookie su `http://100.x`** | non documentato in nessun verso | — | **P1** |
| **Safari: la prevenzione del tracciamento** | il tetto di sette giorni vale per la memoria scrivibile da script e per le risorse secondarie mascherate; **un cookie impostato dal server nella risposta a una pagina di primo livello non ha tetto** | webkit.org/tracking-prevention; dal sorgente (`NetworkTaskCocoa.mm`) | niente |
| **Safari: la schermata Home** | una pagina aggiunta alla schermata Home ha **cookie e memoria separati** da Safari; da iOS 26 ogni pagina aggiunta si apre come web app | WWDC23 10120; webkit.org/blog/17333 | se la web app vede il cookie di Safari: **P1** (atteso: no) |
| **Safari: `http`** | l'avviso per i siti `http` è **facoltativo** (Impostazioni, «Avviso connessione non sicura»); Safari preferisce `https` e torna a `http` se fallisce, ma **non prova `https` su un indirizzo IP** | support.apple.com/guide/iphone/iphfba2ed790 (iOS 27); Safari 18.2 release notes; dal sorgente (`ResourceRequestBase.cpp`) | che cosa mostra Safari sull'iPhone dell'utente: **P0** |
| **Web Push** | vuole iOS 16.4, una web app della schermata Home, JavaScript per l'iscrizione e un contesto sicuro: `http://100.x` non lo è | webkit.org/blog/13878, /16535; W3C Push API, Secure Contexts | niente: escluso (fuori scope) |
| **Tailscale sull'iPhone** | mentre è acceso, Tailscale imposta una regola «VPN on demand» che la tiene attiva; iOS ne tiene una sola alla volta. Private Relay non tocca il traffico di una VPN | tailscale.com/docs/features/client/ios-vpn-on-demand; WWDC21 10096 | niente |
| **`tailscale cert`** | il certificato è per `macchina.tailNNNN.ts.net`, e **i nomi delle macchine e della tailnet finiscono in un registro pubblico**; scade in 90 giorni e il rinnovo è dell'utente | tailscale.com/kb/1153 | niente: escluso (dec. D) |
| **`tailscale serve`** | un reverse proxy verso `127.0.0.1` | tailscale.com/kb/1242 | niente: già escluso (ADR 0037, A3) |
| **ntfy.sh, il server pubblico** | gli argomenti sono pubblici: «the topic is essentially a password». Gratis, 250 messaggi al giorno, cache di 12 ore. Un argomento riservato, con i permessi, costa almeno il piano Supporter | docs.ntfy.sh/publish; ntfy.sh/v1/tiers | niente |
| **ntfy: i prezzi** | Supporter $6 al mese o $60 all'anno (2.500 al giorno, 3 argomenti riservati); Pro $12 al mese o $120 all'anno | ntfy.sh/v1/tiers, 2026-09-19 | la scelta di Tommaso |
| **ntfy: il tocco** | dalla versione iOS 1.6.0 (maggio 2026) il tocco **apre l'URL del tocco** (`Click`) | docs.ntfy.sh/releases | che cosa apre davvero: **P4** |
| **ntfy: i pulsanti e la priorità** | su iOS funzionano i pulsanti `view` e `http`; la priorità 4 diventa «Time Sensitive», la 5 un avviso critico nella versione non ancora uscita | docs.ntfy.sh/publish; dal sorgente di ntfy-ios | niente: nessun pulsante (punto fermo 5), priorità costante (punto fermo 6) |
| **ntfy: la cifratura** | **nessuna cifratura da capo a capo** (issue #69 aperta); l'informativa dice che contenuto e metadati «may be transmitted through Google's FCM infrastructure» | github.com/binwiederhier/ntfy/issues/69; docs.ntfy.sh/privacy | niente: il testo è del vocabolario (dec. E) |
| **ntfy sul nostro Mac** | con `upstream-base-url` a ntfy.sh va soltanto l'id del messaggio e lo SHA-256 dell'URL dell'argomento, e l'iPhone prende il testo dal nostro server. **Ma il server di ntfy su macOS non è supportato**: «Only the ntfy CLI is supported on macOS»; la formula di Homebrew costruisce il solo client | docs.ntfy.sh/config, /install | niente: escluso (dec. E) |
| **Pushover** | $4.99 una volta per piattaforma, prova di 30 giorni; 10.000 messaggi al mese **per account** dal 2026-05-01. **Cifratura da capo a capo facoltativa** dalla versione 5.0 delle app, che copre messaggio, titolo e URL. Il tocco **apre l'app**, e l'URL si tocca lì sotto. La priorità 2 («emergency») ripete fino a conferma | pushover.net/pricing, /api; support.pushover.net/i46; blog.pushover.net 2026/4 | niente |

### Che cosa solo l'iPhone può dire

| | Misura | Che cosa decide |
|---|---|---|
| **P0** | l'iPhone e la tailnet: la versione di iOS; Safari che apre `http://<Mac>:8351/health` e riceve il 401 del middleware; che cosa mostra Safari per una pagina `http` | se il criterio di fine è raggiungibile com'è scritto; dec. D (http e non HTTPS) |
| **P1** | Safari e il cookie, con una sonda usa-e-getta sul Mac: un cookie `HttpOnly; SameSite=Strict` su `http://100.x…` torna indietro dopo una ricarica, dopo aver chiuso Safari, dopo il riavvio dell'iPhone; se una pagina aggiunta alla schermata Home lo vede; se un modulo `POST` porta l'header `Origin` | dec. C (il cookie come portatore), la difesa dal modulo di un altro sito |
| **P2** | lo Shortcut: `Ottieni contenuto dell'URL` con un header verso `http://100.x…`, una risposta JSON; un testo di prova scritto **dentro** uno Shortcut compare sul Mac (sincronizzazione iCloud); un file salvato «Sul mio iPhone» e riletto da uno Shortcut | dec. A (che cosa uno Shortcut recita), dec. C (dove lo Shortcut tiene il segreto) |
| **P3** | il codice che attraversa: copiato nel terminale del Mac, incollato sull'iPhone con gli Appunti universali; se arriva intero, e in quanto tempo | dec. C (come il codice arriva sull'iPhone), la scadenza di dieci minuti (ADR 0037 §5) |
| **P4** | il campanello: l'app ntfy, un argomento di prova su ntfy.sh, una notifica mandata dal Mac con `curl`; che cosa compare sul blocco schermo, quanto ci mette, che cosa apre il tocco | dec. E |

I passi, per chi non è esperto, sono in appendice. Ognuno scrive la sua uscita in un file in
`~/Downloads`: una misura senza il suo file non è una misura.

## I punti fermi, riletti contro i fatti

**Nessuno è smentito da un fatto.** Alcuni portano una tensione che va decisa, e la scrivo invece di
aggirarla.

1. **Le pagine in Safari entrano.** Si possono fare senza JavaScript: un modulo HTML fa `GET` e
   `POST`, e il server compone la pagina. Ne discende un fatto che la ricognizione non poteva
   evitare: **un modulo non sa mettere un header `Authorization`**, quindi Safari porta l'identità
   in un cookie, e il middleware impara un secondo portatore (dec. C). M17.1 aveva scritto per sé
   «Nessuna rotta in `ela.api`, nessun file statico servito dal Core» (`M17.1.md:1034-1035`, fuori
   scope 3): qui entrano entrambe le cose, ed è dec. D. E un criterio che la registrazione della Fase
   17 ha fissato per M17.2 cambia con loro (dec. D, «La 5.10, rivista»).
2. **Il companion è un client dell'API, e chi sceglie scrive.** Regge: il «sì» della pagina passa
   dalla funzione di `api/approvals.py` che la regola 19 tiene unica, e `responded_by` è già
   l'identità risolta (`api/approvals.py:78-83`). Per un'approvazione l'attore è `USER` **per
   costruzione** — `_user_actor` lo compone da `responded_by` (`tasks/engine.py:1158-1162`), e così
   fa `AUTHORIZATION_GRANTED` con `granted_by` —: ciò che cambia è **l'id**, quello dell'iPhone al
   posto di quello di `local`. `Identity.actor` conta invece per la cancellazione
   (`api/tasks.py:145`), e lì il companion firma `USER` con il suo id, non `DEVICE`, perché «chi
   approva è una persona e non un dispositivo» (ADR 0037 §15). «Mai un secondo accesso al database»
   ha una difesa sua, una regola (dec. D). **Due tensioni.** La prima: ADR 0037 §2 appoggiava «un
   «sì» entra da una porta sola» sul fatto che la porta restasse del token del Core — «un'identità di
   nodo che potesse rispondere sarebbe un secondo portatore del «sì»» (`0037-node-identity.md:125`)
   —, e il companion è quel secondo portatore, voluto: la funzione resta una, i portatori diventano
   due. La tabella di ADR 0037 §2 «va riscritta, non aggiornata» (`:133`), e la riscrive ADR 0043,
   voce per voce. La seconda: un «sì» porta il task a `QUEUED`, e nessuno lo fa ripartire se non
   `POST /tasks/{id}/run`. Un «sì» dall'iPhone che aspetta qualcuno al Mac non rende ELA usabile
   lontano dal Mac: è **dec. H**.
3. **Vedere e rispondere, non comandare.** Regge. Due fatti: fermare un task è già una rotta
   (`POST /tasks/{id}/cancel`), ed è **dec. I**, come l'utente ha chiesto; e **vedere un'approvazione
   vuol dire leggere contenuto dell'utente** — lo scopo dello step sta nella domanda. ADR 0037 §2
   aveva scritto che `GET /tasks/{id}` regge sulla tailnet solo perché un'identità di nodo non la
   raggiunge. Il companion non rovescia l'elenco dei nodi: aggiunge un'identità con il suo. Ma la
   domanda «quale contenuto può andare sull'iPhone» ha già una risposta nel repository — la
   sensibilità del task, `LOCAL_ONLY` per default — ed è **dec. F**.
4. **Il campanello non è una lettera.** Regge, e M17.1 l'aveva già preparato: «La chiave è
   l'etichetta, ed è ciò che resta dell'identità dove non arriva nessun CSS — il testo di una
   notifica push» (`M17.1.md:733-735`). Il testo si compone dalle chiavi di `tokens.json`. Un fatto
   in più (dec. E): su ntfy.sh pubblico il nome dell'argomento è una credenziale, e va trattato come
   il segreto di un nodo.
5. **Il «sì» non passa da un server di terzi.** Regge: nessun pulsante d'azione, e l'URL del tocco
   è **fisso** — la pagina iniziale del companion — senza credenziale e senza nemmeno l'id
   dell'approvazione (dec. E).
6. **Niente enum dei livelli di attenzione.** Regge: la priorità che il fornitore riceve è una
   costante dell'adattatore, non un livello (dec. E). **La tensione:** il vocabolario del campanello
   proposto in dec. E è un enum di una voce sola, la forma che questo punto rifiuta per l'attenzione;
   dec. E dice perché lì la ragione è diversa, e offre l'alternativa senza enum (domanda 7).

## Le decisioni

### A. L'iPhone è un nodo? — *proposta A2: un'identità del registro, con un ruolo imposto, che non prende lavoro*

**L'ipotesi dell'utente regge, e il repository l'aveva pianificata diversamente.** La suite di M12.2
dice di sé «This file is the artefact M12.3–M12.5 inherit. They write a `NodeDriver` for their
platform — the macOS node, the Windows Power Node, the iPhone companion» (`tests/conformance/
driver.py:3-4`), `KITS` porta «M12.5 appends its own here» (`test_node_contract.py:67-69`), e il
criterio di fine di M12.2 dice «le tre implementazioni reali, M12.3–M12.5 (macOS, Windows, companion
iPhone), si misureranno contro la suite» (`M12.2.md:96-99`).

**Che cosa sa fare un iPhone, per il protocollo.** Nessun processo suo (la 5.1: un trasporto che
chieda al nodo «di far girare un processo suo taglia fuori l'iPhone»); nessun heartbeat regolare,
perché un'automazione di Comandi non ha un intervallo di minuti — «Ora del giorno» si ripete ogni
giorno, settimana o mese, e gli altri inneschi sono eventi (la tabella della ricognizione); nessuna
attesa lunga affidabile, perché il tempo di «Ottieni contenuto dell'URL» non è documentato; nessun
`Tool.execute`, perché lo Shortcut non ha i tool di ELA.

**Le tredici storie, lette per il companion:**

| Storia | Per un'identità che non prende lavoro |
|---|---|
| 1 arruolati, annuncia, chiedi, esegui, riporta | si arruola sì; non annuncia, non chiede, non esegue, non riporta |
| 2 non chiedere mai | vera per costruzione, ma la storia prova la scadenza di un'**offerta**, e al companion non se ne fa nessuna |
| 3 taci · 4 torna tardi · 7 consegna due volte · 8 consegna ciò che non è tuo · 10 lavora più del TTL · 11 il Core muore a metà | ognuna presuppone un lavoro preso |
| 5 torna in due | presuppone l'annuncio con `If-Match`: il companion non annuncia |
| 6 chiedete insieme | presuppone la presa |
| 9 vieni revocato a metà | «a metà» di un lavoro; la revoca in sé il companion la recita (C6 qui sotto) |
| 12 un task non dichiarato non ti arriva · 13 ciò che si verifica solo qui non ti arriva | a un companion **non arriva niente**, qualunque task: le due storie diventano una, più forte (C3) |

Un kit del companion con tredici voci in `UNSUPPORTED` sarebbe un driver che non recita niente:
**uno stub**, come dice l'utente. Quindi il criterio di fine di M12.2 è **letteralmente falso per
M12.5**, e si corregge apertamente: M12.3 e M12.4 si sono misurate contro la suite dei nodi; **M12.5
si misura contro il contratto del companion**, che nasce qui. ADR 0038 non si riscrive: la
correzione sta in ADR 0043 e qui. Le frasi della suite che promettono il contrario si correggono nel
commit che scrive il contratto del companion; sono in `tests/conformance/__init__.py:4`,
`driver.py:3-4` e `:21`, `test_node_contract.py:1-6`, `:10`, `:26-30` e `:67-69`, `fake_node.py:3-4`
e `:284-285`, `test_unsupported.py:38-42` e `:90-98`. `M12.2.md:1258-1260` riceve una nota che
rimanda qui, come `M12.4.md` l'ha ricevuta per le sue frasi superate.

**Le forme possibili:**

| | Che cos'è l'iPhone | Il costo |
|---|---|---|
| A1 | un nodo come gli altri, con un kit e una mappa `UNSUPPORTED` | lo stub di sopra |
| **A2** | **una riga del registro con un ruolo imposto**: arruolata con un codice, revocabile, con il suo segreto e i suoi eventi `DEVICE_*`; il ruolo decide **da quale portatore** il middleware accetta la sua credenziale e **quali rotte** raggiunge; la metà dichiarata la scrive all'arruolamento e non la riscrive mai | una colonna imposta in più; due identità in più nel middleware |
| A3 | un'identità fuori dal registro, con una tabella sua | rifà arruolamento, revoca, hash e audit che esistono; e §16 della spec e §11 del design vogliono l'iPhone fra i dispositivi |

**Raccomandazione: A2.** È la forma che ADR 0037 §4 aveva preparato: «questo nodo può rispondere
alle approvazioni» è un permesso che l'utente dà a un nodo preciso, **della specie di `privacy`**.
Il ruolo sta nella **metà imposta** (D3), lo sceglie l'utente quando conia il codice (`ela node
enroll --role …`), il codice lo porta, e il nodo non lo scrive mai: un corpo che lo porta riceve 422
come per `privacy`. Ogni ruolo ha un produttore in produzione — l'arruolamento con quel ruolo:

| Ruolo | Chi | Portatore della credenziale | Rotte |
|---|---|---|---|
| **`WORKER`** | ogni nodo di oggi, e `local` — il default della migrazione | l'header | `NODE_ROUTES`, come oggi |
| **`COMPANION`** | Safari sull'iPhone | **solo il cookie** | le pagine: `COMPANION_ROUTES` |
| **`GLANCE`** | lo Shortcut | **solo l'header** | il solo riassunto, `GET /companion/summary` |

**Perché lo Shortcut ha un ruolo suo.** Il segreto dello Shortcut vive in un file (C.2), una custodia più debole di
un cookie `HttpOnly`: un file dell'app File si vede, si sposta, e forse finisce in un backup. **Le
rotte seguono la custodia**: un segreto che può viaggiare più lontano raggiunge soltanto dei
conteggi senza contenuto, e non risponde. Un segreto presentato dal portatore sbagliato — il
segreto di Safari in un header, quello dello Shortcut in un cookie — riceve lo stesso 401 e un
`DEVICE_REJECTED` `route_not_allowed`: altrimenti copiare il segreto dello Shortcut in un cookie gli
aprirebbe le pagine. Il nome `COMPANION` viene da §6 («Companion Node»); `GLANCE` dice ciò che fa, un
colpo d'occhio.

**La metà dichiarata** la scrive il companion all'arruolamento — `name`, `os` `IOS`, nessuna
capability, nessun tool — e non la riscrive, perché `PUT /nodes/me` non è fra le sue rotte. Un
arruolamento `COMPANION` o `GLANCE` che dichiara capability o tool, o un `os` che non è `IOS`,
riceve 422: un companion che dicesse di saper eseguire qualcosa mentirebbe nel registro, anche se
nessun lavoro potrebbe arrivargli. Chi scrive `name` e `os` per ciascun portatore è in C.5.
`--privacy` resta **obbligatoria per ogni ruolo** (ADR 0037 §5: il livello è «always declared»):
per `COMPANION` è il tetto di ciò che vede (dec. F.2), e per `GLANCE`, che non vede contenuto, non
decide niente — ma un default sarebbe proprio la leva che ADR 0037 §5 ha tolto.

**L'orchestratore non cambia, e un companion non è mai idoneo.** Giudica ogni riga di `devices()`
come oggi (D19: nessun filtro prima dei rifiuti). Un companion è scartato **sempre** con
`UNAVAILABLE`, perché la sua disponibilità derivata è quella di chi non manda heartbeat (dec. B), e
in più con `MISSING_TOOL` quando lo step vuole un tool. Il «sempre» conta: uno step senza capability
ha l'insieme dei tool vuoto e `MISSING_TOOL` non scatta (`devices/orchestrator.py:347-348`), e se il
companion risultasse disponibile il runner aprirebbe task e step a suo nome
(`executive/runner.py:265-269`) prima che l'executor rifiuti lo step. Nessun membro nuovo in
`Refusal`. Il prezzo, detto: ogni `DEVICE_SELECTED` nomina i companion fra i candidati scartati.

**Che cosa recita uno Shortcut, davvero:** tre cose, ciascuna **una richiesta, un header, un
JSON** — si arruola (`POST /nodes/enroll` con un codice `GLANCE`, un corpo JSON, la risposta
`{device_id, secret, revision}`); legge il riassunto (`GET /companion/summary`, dec. D); e, revocato,
riceve il 401 JSON del middleware. Niente altro: la risposta si dà sulla pagina (punto fermo 5).

**Il contratto del companion**, in `tests/conformance/`, con **un driver per portatore** — l'header
con un'identità `GLANCE`, che è ciò che manda uno Shortcut, e il cookie con i moduli e un'identità
`COMPANION`, che è ciò che manda Safari. Le storie sono scritte una volta; quelle che un portatore
non può recitare **per ruolo** lo dichiarano con la forma di `UNSUPPORTED`, appuntata, e la ragione
è il ruolo, non la piattaforma:

| # | Storia | Che cosa deve essere vero | Portatori |
|---|---|---|---|
| C1 | **arruolati con il tuo ruolo** | un codice coniato con un ruolo si consuma con una richiesta, sulla rotta di quel ruolo (C.5); la riga nasce con il ruolo, `IOS`, senza capability né tool; il segreto torna una volta — nel corpo per lo Shortcut, in un `Set-Cookie` per Safari | entrambi |
| C2 | **una richiesta basta** | ogni rotta del ruolo risponde a **una** richiesta con **una** credenziale: nessun `If-Match`, nessun `ETag` da rileggere, nessuna attesa lunga; il riassunto risponde JSON. Derivata dall'elenco delle rotte, non scritta a mano | entrambi |
| C3 | **a un companion non arriva niente** | un task `TRUSTED` cammina, anche con uno step **senza capability**: il companion compare fra i candidati scartati con `UNAVAILABLE`, e con `MISSING_TOOL` dove lo step vuole un tool; nessun task e nessuno step si apre a suo nome; sulle rotte del lavoro riceve 401 e `DEVICE_REJECTED` `route_not_allowed` | entrambi |
| C4 | **rispondi, e firma chi sceglie** | con il «sì» della pagina e il `run` che lo segue (dec. H): `responded_by` e l'id dell'attore di `APPROVAL_RESOLVED` e di `AUTHORIZATION_GRANTED` sono **l'id del companion**, non quello di `local` | cookie |
| C5 | **vedi solo ciò che il tuo tetto ammette** | per un task `LOCAL_ONLY` nessuna pagina porta i campi di dec. F.2, e il «sì» da lì è rifiutato | cookie |
| C6 | **vieni revocato** | l'utente revoca con una domanda in attesa: la richiesta successiva riceve 401 e `DEVICE_REJECTED` `revoked`; Safari riceve il cookie cancellato (C.3); la domanda resta rispondibile dal Mac | entrambi |
| C7 | **ognuno sulle sue rotte, con il suo portatore** | un nodo sulle rotte del companion, un companion sulle rotte dei nodi, il segreto di Safari in un header, quello dello Shortcut in un cookie, il token del Core su una rotta del companion: 401, e dove l'identità esiste `DEVICE_REJECTED` `route_not_allowed` | entrambi |
| C8 | **fermati** (se dec. I entra) | `TASK_CANCELLED` firmato `USER` con l'id del companion, **non** `DEVICE`: qui l'attore viene da `Identity.actor`, e il negativo può fallire | cookie |
| C9 | **il campanello non dice niente di tuo** | per un task con uno scopo e degli argomenti riconoscibili, ciò che il fornitore finto riceve è una voce del vocabolario chiuso, e nient'altro (dec. E). Una volta sola: non dipende dal portatore | — |

**Che cosa questo contratto non può provare**, nella forma di ADR 0038 §18: uno Shortcut non si
guida da `pytest`, Safari nemmeno, e un cookie vero non si prova con un client HTTP; la sincronia di
iCloud, gli Appunti universali e il tocco di una notifica non sono qui. Verde qui vuol dire che il
Core e i due portatori dicono la stessa cosa del contratto; verde **sull'iPhone** è la prova a mano.

### B. Che cosa vuol dire «connesso» per un iPhone senza heartbeat — *proposta B2: il contatto è il fatto, la disponibilità resta quella dell'heartbeat*

La disponibilità di un nodo oggi è **derivata** dall'heartbeat (ADR 0016 §3): `last_seen_at` entro
il TTL, e non revocato. Un companion non manda heartbeat.

| | Che cosa si scrive | Che cosa si mostra | Il difetto |
|---|---|---|---|
| B1 | niente | «mai visto» | **falso**: l'iPhone parla con ELA ogni giorno |
| **B2** | **ogni richiesta autenticata di un companion o di uno Shortcut fa scrivere al registro `last_seen_at`** — la metà osservata, senza audit, come l'heartbeat (ADR 0016 §6); **la disponibilità derivata di un'identità che non è `WORKER` è `UNAVAILABLE`**, qualunque sia l'ultimo contatto | «companion · ultimo contatto 10:42» | nessuno che conosca: un fatto passato, detto come passato |
| B3 | come B2 | «Connected» se il contatto è più recente di N minuti | una congettura sul presente, con una soglia che nessuno ha misurato |

**Raccomandazione: B2.** Due fatti, due nomi, come ADR 0037 §12 ha fatto per la revoca. **L'ultimo
contatto** è `last_seen_at`: quando quell'identità ha parlato con ELA l'ultima volta — per un nodo
parlare è l'heartbeat, per un companion è una pagina o un riassunto. **La disponibilità** resta
«il fatto dell'heartbeat», che è ciò che ADR 0037 §12 dice di `seen()` (vincoli dichiarati, `:898-
900`): un companion non ne manda, quindi è `UNAVAILABLE` per costruzione, e la diagnosi F1 — «No
heartbeat within the TTL» (`devices/orchestrator.py:123`) — dice il vero. `available()` ne segue, e
`GET /devices` porta `available: false`, `role` e `last_seen_at`; `ela device list` e le pagine
dicono «ultimo contatto», mai «connesso» né «disponibile». L'esempio di §11 del design — «iPhone ·
COMPANION NODE · Stato: Connected» — è un esempio, non un contratto: M17.2 lo renderà con questa
derivazione.

Una disponibilità derivata dall'ultimo contatto, invece, direbbe `AVAILABLE` per un minuto dopo ogni
pagina aperta, e in quel minuto il companion sarebbe **idoneo** per uno step senza capability (dec.
A): è la ragione per cui la condizione sul ruolo entra nella derivazione e non solo in `available()`.

**Un effetto voluto:** l'ultimo contatto è anche un segnale per l'utente. Un iPhone che ha parlato
con ELA alle tre di notte è una cosa da vedere in `ela device list` prima di revocarlo.

### C. Come si autentica Safari, dove sta il segreto, e come arriva il codice — *proposta*

**C.1 — Safari: il cookie porta la credenziale.** `ela_companion=<device_id>.<segreto>`, con
`HttpOnly`, `SameSite=Strict`, `Path=/companion`, `Max-Age` di 400 giorni — il tetto che la bozza
RFC 6265bis chiede ai browser: passato, l'iPhone si riarruola —, e **senza** `Secure`, che Safari 27
accetta su `http` solo per il loopback (la tabella della ricognizione). Lo imposta **una volta** la
risposta della pagina di arruolamento, e da lì non compare più in nessun corpo, URL, log o evento:
la regola 46 lo impara per nome, e la 47 impara che solo `api/security.py` legge il cookie come
legge l'header. Un segreto per identità (D5), un hash, una revoca.

- **Perché la credenziale e non una sessione derivata:** una sessione vorrebbe una tabella, una
  scadenza e un rinnovo, cioè il macchinario che ADR 0037 ha scartato con l'HMAC; e revocare
  l'identità revoca già il cookie, alla richiesta successiva.
- **`Path=/companion`**, perché Safari chiede da sé `/favicon.ico` e `/apple-touch-icon*.png` — lo
  ha visto la prova a mano di M17.1 (`M17.1.md:1191`). Con il cookie su `/`, ogni visita scriverebbe
  nell'audit un `DEVICE_REJECTED` `route_not_allowed`; con il cookie sul percorso del companion
  quelle richieste restano anonime, contate e non scritte (ADR 0037 §13). Per la stessa ragione i
  due fogli di stile stanno sotto il percorso: `/companion/tokens.css`, `/companion/components.css`.
- **Un modulo di un altro sito:** `SameSite=Strict` tiene il cookie fuori da una richiesta che parte
  da un'altra pagina, e ogni `POST` **del portatore cookie** controlla in più che `Origin` sia
  l'origine di ELA (P1 verifica che Safari lo mandi); lo Shortcut non ha rotte `POST` oltre
  all'arruolamento, e non manda `Origin`. Il default non aiuta: Safari 18.0 ha tolto il `Lax`
  implicito, quindi `Strict` si scrive, e un test lo legge nel `Set-Cookie`.
- **Una pagina aggiunta alla schermata Home** ha cookie e memoria separati da Safari, e da iOS 26
  ogni pagina aggiunta si apre così: il cookie di Safari lì non c'è (P1 lo verifica). Il tocco di una
  notifica apre **Safari**, non la pagina aggiunta: il companion si arruola in Safari, e la schermata
  Home resta fuori scope.

**C.2 — Lo Shortcut: un'identità sua, `GLANCE`, e il segreto in un file sul telefono.** Un cookie
`HttpOnly` non si legge da uno Shortcut, e il segreto non passa mai da una pagina che lo mostri:
quindi lo Shortcut **si arruola da sé**, con un suo codice, e ha **una riga sua** — revocabile da
sola, con il suo ultimo contatto. Un segreto scritto **nel testo** dello Shortcut andrebbe dove va lo
Shortcut: su ogni dispositivo dell'utente con la sincronizzazione iCloud, con le chiavi presso Apple
se la Protezione dati è quella standard; in ogni copia condivisa, perché un campo viaggia com'è
scritto se non ha una domanda di importazione; e ad Apple stessa, se lo Shortcut si condivide come
file con «Chiunque» (la tabella della ricognizione). Per questo il segreto **non sta nel testo dello
Shortcut**: lo Shortcut lo salva in un file «Sul mio iPhone» al primo avvio e lo rilegge a ogni
richiesta, così che sincronizzare, esportare o condividere lo Shortcut non porti con sé niente. P2
misura se regge. Che cosa porti con sé un **backup** dell'iPhone su iCloud — che con la Protezione
dati standard ha le chiavi presso Apple — non l'ho stabilito, né per quel file né per i cookie di
Safari: dichiarato. È la ragione per cui lo Shortcut è `GLANCE` e non `COMPANION` (dec. A).

*Alternativa scartata:* **una riga sola per l'iPhone, con due segreti.** Un nodo con due
credenziali vuole una tabella dei segreti accanto a `devices` — la G3 che ADR 0037 §8 ha scartato
perché due posti possono dire cose diverse.

**C.3 — Il 401 del companion, e come Safari presenta il codice.** Il codice è una credenziale che il
middleware riconosce (ADR 0037 §3), e una pagina non ha un header da mettere. Le regole, che il
middleware applica in quest'ordine:

1. **`POST /companion/enroll`** risolve **solo** il campo `code` del modulo: né un header né un
   cookie su quella richiesta si guardano, perché un iPhone con un cookie revocato deve potersi
   riarruolare. Il middleware legge quel campo, su quella rotta sola, e lo risolve come oggi risolve
   `Bearer <codice>`: nessuna rotta fuori dal middleware (D10). La risposta è un `303` verso la
   pagina iniziale, con il `Set-Cookie`; un codice che non si risolve riceve il 401 della pagina
   della regola 3.
2. **Una richiesta con un header `Authorization`** si risolve come oggi, e **un suo rifiuto è il 401
   JSON di oggi**, su qualunque percorso: lo Shortcut compreso. Il token del Core su una rotta del
   companion è contato con un motivo suo, `core_on_a_companion_route` — `core_on_a_node_route`
   (`api/security.py:111`) mentirebbe nel nome.
3. **Una richiesta senza header su un percorso che comincia con `/companion/`** si risolve dal
   cookie. Se **la credenziale stessa** fallisce — assente, sconosciuta, con il segreto sbagliato,
   revocata — riceve **il 401 della pagina**: il modulo di arruolamento, **sempre lo stesso**,
   qualunque sia il percorso sotto il prefisso, con `Set-Cookie: ela_companion=; Max-Age=0;
   Path=/companion`. Un cookie revocato scrive così **un** `DEVICE_REJECTED` `revoked` — nomina un
   nodo che esiste — e poi non c'è più. Se invece la credenziale è buona e la rotta non è del suo
   ruolo — un percorso scritto male, il riassunto dello Shortcut — la risposta è **la stessa pagina
   senza cancellare il cookie**, e un `DEVICE_REJECTED` `route_not_allowed`: altrimenti un indirizzo
   sbagliato chiuderebbe fuori l'iPhone, e per rientrare servirebbe un codice nuovo, che si conia
   solo al Mac.
4. Tutto il resto riceve il 401 JSON di oggi.

I 401 della pagina restano uguali **per chi non ha una credenziale valida**: la sola differenza, il
cookie cancellato o no, la vede soltanto chi ne presenta una buona, che sa già di averla.

**Questo rivede ADR 0023 §7, e lo dico.** «Chi non ha il token non impara nemmeno quali rotte
esistono» (`0023-composition-root-and-api.md:223`): con la regola 2 chi bussa senza credenziali
impara che **il prefisso** `/companion/` è una pagina che chiede un codice. Non impara quali rotte ci
sono sotto, perché ogni percorso del prefisso risponde la stessa pagina. Il JSON del 401 di oggi dice
già che dietro c'è ELA. ADR 0043 lo scrive come revisione, con un test per ciascuna delle regole.

*Scartate:* il codice nell'URL — finirebbe nella cronologia di Safari e con lei su ogni dispositivo
dell'utente (cifrata da capo a capo anche con la Protezione dati standard, ma pur sempre una
credenziale ricopiata), e un segnalibro, che ha le chiavi presso Apple, lo porterebbe fuori; e
l'autenticazione `Basic` del browser, che non vuole un modulo ma che un browser ripresenta da sé
alle richieste successive dello stesso spazio (RFC 7617 §2.2): ogni pagina porterebbe un codice
consumato, e ogni volta sarebbe un `DEVICE_REJECTED` `code_reused`.

**C.4 — Come il codice arriva sull'iPhone.** ADR 0037 §5 lo lascia a M12.5 («come lo riceva uno
Shortcut il repository non lo dice, ed è M12.5»). Il codice è di 43 caratteri, «si incolla, non si
digita». **Proposta: gli Appunti universali** — copiato a mano dal terminale del Mac, incollato
sull'iPhone. ADR 0037 li aveva scartati per un nodo che ha uno stdin, perché «con Handoff passano
agli altri dispositivi Apple»: per l'iPhone **passare a un altro dispositivo è esattamente ciò che
serve**, il codice è monouso e scade in dieci minuti. Un dettaglio che solo la misura dice: `ela node
enroll` stampa il codice **dentro una tabella**, accanto alla privacy e alla scadenza
(`cli/nodes.py:101-107`), e `token_urlsafe` contiene `-` e `_`, che un doppio clic nel Terminale può
non prendere. P3 lo misura con la stessa tabella; se il doppio clic taglia il codice, `ela node
enroll --role companion|glance` stampa il codice su una riga sua. P3 misura anche la scadenza che ADR
0037 §5 chiedeva «soprattutto dallo Shortcut».

**C.5 — L'arruolamento, codice per rotta.** Il ruolo lo porta il codice, e ogni rotta di
arruolamento accetta soltanto i codici del suo portatore. Un codice rifiutato **non si consuma**: il
controllo del ruolo viene prima dell'`UPDATE` condizionale di ADR 0037 §5.

| Codice | Rotta | Esito |
|---|---|---|
| `WORKER` | `POST /nodes/enroll` | `201`, un nodo, come oggi |
| `GLANCE` | `POST /nodes/enroll` | `201`: la riga dello Shortcut; il corpo JSON dichiara `name` e `os` `IOS`; capability, tool o un altro `os` → `422` `invalid` |
| `COMPANION` | `POST /nodes/enroll` | `422` `invalid`: la credenziale di un companion non torna mai in un corpo, nasce in un `Set-Cookie` |
| `COMPANION` | `POST /companion/enroll` | `303` e il `Set-Cookie`; il modulo dichiara `name` — scritto dall'utente, «iPhone» per default — e `os` `IOS`, che la pagina porta in un campo visibile; un altro `os` → `422` sulla pagina |
| `WORKER`, `GLANCE` | `POST /companion/enroll` | `422` sulla pagina, con il comando che conia il codice giusto |
| `WORKER` usato dallo Shortcut | `POST /nodes/enroll` | `422` `invalid` se dichiara `os` `IOS`: un nodo che lavora non gira su un iPhone (la 5.1). Altrimenti nasce un nodo che non chiederà mai lavoro, si vede in `ela device list` e si revoca: dichiarato |

### D. La pagina: chi la serve, come parla con l'API, e niente JavaScript — *proposta D1*

| | Chi compone la pagina | Il costo |
|---|---|---|
| **D1** | **`ela.api` stessa**: pagine composte sul server, moduli HTML, niente JavaScript; lo stesso processo, lo stesso middleware, le stesse funzioni delle rotte JSON. Il browser è il client | `ela.api` impara a rispondere HTML |
| D2 | un processo a parte che chiama `ela.api` con la credenziale dell'iPhone, come la CLI | un secondo server sulla tailnet che tiene e inoltra la credenziale dell'iPhone: il reverse proxy che la 5.2 esclude, con un altro nome |
| D3 | pagine statiche e JavaScript nostro che chiama le rotte JSON | il segreto in una memoria che ogni script della pagina legge; codice che decide dentro `apps/`, che riapre il gate di ADR 0042 §10; e nessun browser in CI che lo provi |

**Raccomandazione: D1.** «Client dell'API come la CLI» ha una ragione scritta, ed è quella che
conta: «`ela.api` è l'unica porta da cui un «sì» dell'utente entra nel sistema … un Command Center
che leggesse il database sarebbe un secondo ELA» (`docs/STATO.md`, 5.10). D1 la tiene se la pagina è
**un'altra rappresentazione delle stesse rotte**, dietro lo stesso middleware, e **una regola lo
rende vero invece che promesso**: *le pagine leggono le rotte* — `api/companion.py` chiama soltanto
le funzioni delle rotte JSON di `ela.api` e le forme di `api/schemas.py`, e non nomina mai un port,
uno store, il catalogo o l'executor attraverso `Ela`. È la forma della regola 28 della CLI
(`cli-over-the-api`), con il suo caso negativo. Il tetto del companion non ha bisogno di una lettura
in più: il middleware ha già in mano la riga quando controlla la revoca (`api/security.py:224`), e
l'`Identity` che risolve porta il ruolo e la `privacy` dell'identità, come oggi porta il suo id. Ne
discende che ciò che la pagina mostra deve già
stare in una rotta: per questo `ApprovalOut` si estende (dec. F), invece di lasciare alla pagina una
seconda copia di `_stated`. La CLI è un processo a parte perché vive in un terminale; una pagina
vive nel browser, e il suo client è il browser.

**I dettagli della forma, che l'ADR fissa:**

- **http sulla tailnet, non HTTPS.** WireGuard cifra da nodo a nodo — è ciò su cui ADR 0037 §2 ha
  già appoggiato «Nessun TLS». `tailscale serve` è già scartato (ADR 0037, alternativa A3: un reverse
  proxy, e ogni richiesta arriverebbe da `127.0.0.1`). `tailscale cert` con il TLS in uvicorn non
  vorrebbe un proxy, e comprerebbe due cose: un cookie `Secure` e un contesto sicuro. Nessuna delle
  due serve qui — il cookie lo proteggono WireGuard, `HttpOnly` e `SameSite`, e il contesto sicuro
  lo chiedono solo le API di JavaScript, che non usiamo. Il prezzo invece c'è: i nomi delle macchine
  e della tailnet in un registro pubblico, un certificato da rinnovare a mano ogni 90 giorni
  (tailscale.com/kb/1153), e un nome al posto del numero su cui ADR 0037 §2 lega la presa. Fuori
  scope, dichiarato; P0 dice che cosa mostra Safari per `http` sull'iPhone dell'utente.
- **Niente JavaScript, difeso due volte.** I modelli delle pagine non contengono script (un test in
  `tests/ios/`, nella forma di `tests/design/test_no_network.py`), e **ogni pagina risponde con una
  `Content-Security-Policy`** che non ammette script né da fuori né in linea — `default-src 'none';
  style-src 'self'; form-action 'self'; frame-ancestors 'none'; base-uri 'none'` —: se un giorno un
  valore sfuggisse all'escape, il browser si rifiuterebbe di eseguirlo. I frammenti di M17.1 non
  hanno stili in linea, quindi la CSP non li rompe.
- **Solo la libreria standard.** I moduli si leggono con `urllib.parse` (FastAPI vorrebbe
  `python-multipart` per `Form`), l'HTML si compone con un escape **per costruzione** — ogni valore
  passa da `html.escape`, e non esiste un modo di inserirne uno crudo, salvo un frammento del
  repository —; niente Jinja2. `uv.lock` non si muove.
- **Dove vive la cartella: `apps/ios/`**, quella che §48 ha già disegnato e il cui README oggi è un
  segnaposto: i modelli delle pagine e la guida dello Shortcut, **con le regole in `tests/ios/`** —
  niente script, solo classi che `components.css` definisce, titoli senza contenuto —, come
  `CLAUDE.md` vuole per ciò che sta in `apps/`. Il controllo delle classi è lo stesso delle
  composizioni di M17.1
  (`tests/design/test_components.py::test_the_compositions_are_made_of_components_and_of_nothing_else`),
  riusato e non ricopiato. Le rotte stanno in `src/ela/api/companion.py`, e la composizione HTML in un
  modulo suo di `ela.api` che M17.2 riuserà. Il CSS è quello di M17.1: `ela.api` serve **solo**
  `tokens.css` e `components.css` di `apps/design-system/`, in sola lettura, e la sfera si
  **include** da `_orb.html` invece di ricopiarla — il vincolo dichiarato di ADR 0042, «Il markup di
  un componente si ricopia … niente qui si accorge se diverge», per la sfera smette di valere.
- **Il tema è quello scuro, sempre.** ADR 0042 lascia a ogni superficie il seguire
  `prefers-color-scheme` («una riga di ogni superficie»), ma quella riga è CSS: le pagine del
  companion non hanno un foglio loro, il tema chiaro si accende solo con un attributo, e il server
  non sa che cosa preferisce il telefono. Seguire il sistema vorrebbe una regola derivata in
  `tokens.css`, cioè un cambio del generatore di M17.1: dichiarato, e lasciato a M17.2, che decide
  anche il tema del Mac.
- **Il percorso di `apps/` a runtime.** ELA gira dal repository (5.5): il percorso si deriva dal
  package e si controlla all'avvio, e un avvio senza la cartella si ferma con exit 2, come ogni
  configurazione che ELA non può usare. Vincolo dichiarato.
- **Titoli e URL senza contenuto.** La cronologia e i pannelli di Safari viaggiano su ogni
  dispositivo dell'utente — cifrati da capo a capo —, e un segnalibro viaggia con le chiavi presso
  Apple (support.apple.com/102651). Il `<title>` è «ELA», e un URL porta al più un id opaco — mai
  uno scopo, un argomento, un nome.
- **Gli id viaggiano nella query o nel corpo, mai nel percorso.** `COMPANION_ROUTES` è un insieme di
  coppie letterali come `NODE_ROUTES`, per la ragione di ADR 0038 §11: insegnare i template al
  middleware sarebbe «una difesa che sembra attiva».
- **Gli errori sotto `/companion/` sono pagine.** I gestori di `FAILURES` rispondono JSON
  (`api/app.py:101-124`), e in Safari comparirebbe JSON crudo. Sotto il prefisso, lo stesso errore —
  una domanda già risposta dal Mac, un `run` già in corso, un id sconosciuto, il rifiuto di F.2, un
  `Origin` sbagliato — diventa una pagina con lo stesso stato e una frase, composta dallo stesso
  compositore che mette la CSP.
- **La pagina di arruolamento è l'unica senza l'identità.** È il 401, e il CSS sta dietro il
  middleware: chi non è ancora nessuno non lo carica, e la CSP vieta gli stili in linea. Quindi è
  HTML senza stile, con il carattere di default di Safari: dichiarato. L'alternativa — il CSS servito
  senza identità — sarebbe la prima rotta anonima di ELA, cioè un'esenzione (ADR 0037 §3).

**Le pagine**, e ciò che ciascuna mostra:

| Pagina | Che cosa | Da dove |
|---|---|---|
| **la pagina iniziale** (`GET /companion/`) | la presenza di ELA — la sfera e la chiave —; le domande che aspettano; i task vivi con il loro stato; «nient'altro ti aspetta» quando è vero | le funzioni di `GET /approvals` e di `GET /tasks`, e la proiezione qui sotto |
| **un'approvazione** (`GET /companion/approval?id=…`) | dec. F | la funzione di `GET /approvals`, con `ApprovalOut` estesa |
| **la risposta** (`POST /companion/answer`) | `approve` o `deny`; poi `303` verso la pagina iniziale, con l'esito vero del `run` | la funzione di `api/approvals.py`, e quella di `POST /tasks/{id}/run` (dec. H) |
| **fermare** (`POST /companion/cancel`) | solo se dec. I entra | la funzione di `POST /tasks/{id}/cancel` |
| **il riassunto** (`GET /companion/summary`, `GLANCE`) | JSON per lo Shortcut: la chiave dello stato, quante domande, quanti task per stato; **nessun contenuto** — è ciò che Siri dice a voce alta | le stesse funzioni della pagina iniziale |
| **i due fogli di stile** | `/companion/tokens.css`, `/companion/components.css` | `apps/design-system/` |
| **l'arruolamento** (`POST /companion/enroll`) | C.3, C.5 | la stessa funzione di `POST /nodes/enroll` |

**La presenza è una proiezione, e ciò che mostra è una scelta.** Lo stato che la sfera mostra si
calcola a ogni lettura da ciò che le rotte espongono, mai memorizzato (5.10). Proposta: **tre stati
di §6 del design, in quest'ordine di precedenza** — `WAITING APPROVAL` se una domanda aspetta,
`WORKING` se un task è `EXECUTING`, `IDLE` altrimenti. Gli altri che una fonte ce l'avrebbero restano
fuori, con la ragione: `PLANNING` ha `TaskState.PLANNING`, ma oggi un task in quello stato aspetta un
piano scritto a mano, e la sfera direbbe che ELA pianifica mentre aspetta l'utente; `ERROR` ha
`FAILED`, ma un task fallito resta fallito per sempre, e senza una finestra — che nessuno ha misurato
— la sfera resterebbe rossa; `WAITING` vorrebbe sapere se uno step è da un nodo o aspetta un nodo,
che `TaskOut` non dice. È la domanda aperta di `M17.2.md` «quali stati hanno una fonte», e M17.2 la
estende. Un test appunta il codominio dentro le chiavi di `tokens.json`. `OFFLINE` non ha bisogno di
una fonte: se ELA è spenta, la pagina non arriva.

**La 5.10, rivista apertamente — e un criterio di M17.2 con lei.** Oggi la 5.10 dice: «Le parti
tecniche prendono il loro ADR con la milestone che le costruisce: la cartella, lo stack e il calcolo
della proiezione con M17.2». La proposta, da scrivere nella 5.10 dopo l'approvazione e non prima:

> *La forma del client web la decide M12.5, con ADR 0043, e M17.2 la eredita*: le pagine le compone
> `ela.api`, dietro lo stesso middleware, e leggono le sue rotte; l'identità di un browser viaggia in
> un cookie; niente JavaScript nostro, e una `Content-Security-Policy` che lo vieta; i modelli stanno
> in `apps/<superficie>/`. M17.2 decide ancora le sue viste, l'identità del browser del Mac, il tema,
> e gli stati che la proiezione di M12.5 non copre.

**La tensione:** la registrazione della Fase 17 ha fissato per M17.2 tre criteri «che la SPEC non
può togliere» (`M17.2.md:53`), e il primo dice «Il Command Center parla con ELA **solo** attraverso
l'API, con il token, come la CLI» (`:56`; e `:32-33`: «legge e agisce solo attraverso le rotte di
`ela.api`, con il token»). Con D1 un browser porta un cookie, non il token. La proposta è di
riscriverlo così: «Il Command Center parla con ELA solo attraverso `ela.api`, con un'identità che il
middleware risolve, e le sue pagine leggono le rotte: nessuna lettura del database, nessun accesso
al Core che non passi da `ela.api`». Il senso resta, la lettera cambia: è la domanda 8, e senza il sì
dell'utente M17.2 non si tocca. Se lo dà, `M17.2.md` perde anche dalle sue domande aperte «lo
stack» e «dove si calcola la proiezione», con un rimando qui.

### E. Il campanello: ntfy o Pushover, e quali eventi — *proposta, i numeri a Tommaso*

I numeri sono quelli della tabella della ricognizione, letti il 2026-09-19. **Il costo lo decide
Tommaso**; la raccomandazione qui sotto è sulle altre colonne.

| | Fornitore | Costo | Che cosa vede un terzo | Che cosa apre il tocco | Il rischio |
|---|---|---|---|---|---|
| **E1** | **ntfy.sh, un argomento casuale** | **gratis**; 250 messaggi al giorno, cache di 12 ore | la voce del vocabolario, l'URL del tocco — cioè l'indirizzo del Mac sulla tailnet — e l'ora; in chiaro per ntfy.sh, per FCM di Google secondo l'informativa di ntfy, e per APNs | **la pagina**, dalla versione 1.6.0 dell'app | chi conosce l'argomento legge i campanelli e può suonarne di falsi, con un URL suo |
| E1+ | ntfy.sh, un argomento riservato | $6 al mese o $60 all'anno (Supporter) | come E1 | come E1 | come E1, ma solo ELA suona e solo l'iPhone ascolta: niente campanelli falsi |
| E2 | ntfy sul Mac, con l'inoltro a ntfy.sh | gratis | ntfy.sh vede un id e un'impronta, non il testo | la pagina | **escluso**: il server di ntfy su macOS non è supportato, e un container va contro la 5.5 |
| E3 | Pushover, con la cifratura da capo a capo | $4.99 una volta; 10.000 messaggi al mese | **niente**: messaggio, titolo e URL cifrati | **l'app di Pushover**; l'URL si tocca lì, sotto il messaggio | un account e due chiavi da custodire invece di una; due tocchi invece di uno |

**Raccomandazione: E1.** È l'unica che fa letteralmente ciò che il punto fermo 5 chiede — la notifica
apre la pagina — senza un account; e ciò a cui rinuncia rispetto a E3, la cifratura, protegge un
testo che per tipo non porta niente dell'utente. Ciò che resta da proteggere è **l'argomento**, che
su ntfy.sh è una credenziale («the topic is essentially a password»): 128 bit casuali, trattato come
il segreto di un nodo — fuori dall'audit, fuori da ogni URL. L'adattatore **pubblica in JSON alla
radice** di ntfy, con l'argomento nel corpo e non nel percorso (docs.ntfy.sh/publish). Se Tommaso
vuole togliere anche i campanelli falsi, **E1+** è lo stesso adattatore con un token in più; se
preferisce la cifratura e accetta il secondo tocco, **E3**. Nessuna delle tre chiede un processo in
più sul Mac. P4 misura E1 sull'iPhone vero.

**Le impostazioni, e l'argomento che attraversa.** Due variabili nuove, nella forma della tabella di
ADR 0023 §3: `ELA_NTFY_TOPIC`, `SecretStr`, facoltativa — senza, nessun campanello — e
`ELA_NTFY_URL`, default `https://ntfy.sh`. L'argomento è **una credenziale durevole**, a differenza
del codice, e ADR 0024 §8 non lascia stampare un segreto: «un segreto su un terminale è un segreto
nello scrollback e nella cronologia». Quindi il comando che `docs/GETTING_STARTED.md` darà — una
riga di Python, 16 byte di `secrets.token_urlsafe`, che stanno nell'alfabeto degli argomenti di ntfy
— lo **scrive in coda al `.env`**, che è già `0o600`, e lo **mette negli appunti senza stamparlo**;
da lì arriva nell'app ntfy dell'iPhone con gli Appunti universali, come il codice (P3). Per un
momento un segreto durevole sta negli appunti del Mac e passa con Handoff: è il prezzo di non
digitarlo, ed è dichiarato.

**Quali eventi: una sola voce, l'approvazione che aspetta** — come l'utente propende. Ogni
approvazione che nasce suona una volta, e nient'altro suona: né un task finito, né un errore, né un
nodo che sparisce. Il giorno in cui ELA deciderà *se* suonare è §8 e §34, la Fase 15.

**Il vocabolario chiuso, derivato come `WireCode`.** Un `StrEnum` con una voce — l'approvazione che
aspetta — e il testo composto da ELA con le chiavi di `tokens.json`: titolo «ELA», corpo «WAITING
APPROVAL · MEDIUM». Il livello di rischio è l'unico dato variabile, ed è del catalogo, non
dell'utente. **Il port non accetta una stringa**: prende un membro del vocabolario e un `RiskLevel`,
e l'adattatore compone il testo da una tabella sua. Quindi «niente dell'utente nel testo» è vero
**per tipo** — `mypy --strict` rifiuta una stringa, come per `problem()` — e un test lo prova anche
a runtime, con la forma di `tests/api/test_wire_codes.py`: ogni voce ha uno scrittore, ogni testo
emesso è di una voce, e un secondo punto che suona fa fallire il censimento.

*Una voce sola in un enum è la forma che il punto fermo 6 rifiuta per l'attenzione, e lo dico.* La
ragione lì era un valore solo di una lista che la spec scrive più lunga, con gli altri senza
produttore; qui non c'è una lista più lunga di cui questa voce sia la prima, e l'enum è ciò che
rende il testo chiuso per tipo. L'alternativa è un port con un metodo per voce —
`approval_waiting(risk)` —, chiuso dall'interfaccia invece che dall'enum: stessa garanzia, meno
somiglianza con `WireCode`. È una delle scelte della domanda 7.

**Chi suona, e quando.** L'approvazione nasce in `Executor._ask` (`executive/executor.py:1337-1376`):
lì, **dopo** che la domanda è salvata e il task aspetta, l'executor suona attraverso il port. Un
campanello che non suona — la rete, il fornitore — **non** fa fallire niente: la domanda aspetta
comunque sulla pagina e nella CLI, e l'esito si scrive. Una morte fra la domanda salvata e il
campanello lascia una domanda senza campanello: dichiarato.

**Nell'audit, sì.** Il campanello è un dato che esce verso un terzo — che una domanda aspetta, e
quando —, e §57 vuole le quattro risposte. Un tipo di evento nuovo, `BELL_RUNG`, attore `SYSTEM`,
con la voce, il rischio, il fornitore, l'id dell'approvazione e se è stato consegnato; **mai**
l'argomento di ntfy né l'URL.

**Non è una capability, e lo dico perché è discutibile.** §27 vuole «Planner → Capability →
Guardian → Authorization → Tool → Device → Audit» per ogni azione con effetti esterni, e un
campanello un effetto esterno ce l'ha. La proposta è di trattarlo come l'infrastruttura con cui
ELA **chiede** il consenso, non come un'azione verso lo scopo dell'utente: (1) non porta contenuto,
per tipo; (2) chiedere il consenso per poter chiedere il consenso è circolare; (3) la politica è la
configurazione dell'utente — senza argomento configurato, nessun campanello; (4) la scelta *se*
disturbare è §8, la Fase 15. È una delle scelte della domanda 7.

**Il tocco e la priorità.** L'URL del tocco è **fisso**: la pagina iniziale del companion, composta
dall'indirizzo della tailnet **su cui questo processo ascolta davvero** — quello che `api/server.py`
ha legato e che `/diagnostics` mostra (ADR 0037 §2) —, non dall'impostazione: se all'avvio la
tailnet non c'era, ELA ascolta sul solo loopback e un campanello aprirebbe un indirizzo muto.
L'executor non conosce URL — suona un codice, e `ela.executive` non può importare `ela.api` —:
l'URL lo compone l'adattatore, che riceve dalla composizione l'indirizzo legato, dopo il bind e
prima di servire. Senza
l'indirizzo della tailnet il campanello non suona, e `/diagnostics` lo dice. Un indirizzo IPv6 va fra
parentesi quadre nell'URL. Nessun pulsante d'azione (punto fermo 5). La priorità è una costante
dell'adattatore, la stessa per ogni voce (punto fermo 6).

### F. Che cosa mostra la pagina di un'approvazione — *proposta*

**Ciò che si approva nomina ciò che conta.** Voce per voce, contro §14 del design:

| §14 del design | Che cosa mostra la pagina | Da dove | C'è oggi? |
|---|---|---|---|
| cosa vuole fare | la capability, con la sua descrizione del catalogo, e i `targets` | `ApprovalOut`: `capability_id`, `description`, `targets` | la descrizione no |
| perché · che cosa ELA ha capito | lo scopo dello step e gli argomenti dichiarati (`purpose`, `seconds`) | lo step, `prompt_arguments` | nella domanda come frase unica |
| quale capability | l'id della capability | `capability_id` | sì |
| livello di rischio | il rischio **del catalogo**, quello che il Guardian ha usato (`permissions/guardian.py:256`) — non quello che il piano dichiara | il catalogo | **no**: `ApprovalOut` non lo porta |
| dove può andare il dato | la sensibilità del task, come livello («può girare su un nodo `TRUSTED`») | `task.max_privacy` | **no** in `ApprovalOut`; sì nella frase |
| durata | «un uso, entro un'ora dal tuo sì»; la domanda scade il … | `ELA_AUTHORIZATION_TTL_SECONDS`, `expires_at` | **no**: i termini del grant non sono esposti |
| quale dispositivo | **niente**: il nodo non è un fatto al momento della domanda | — | — |
| conseguenze · reversibilità | **niente**: il catalogo non le dice, e una pagina che le scrivesse le inventerebbe. La composizione di M17.1 («not reversible») è un esempio | — | dichiarato |
| cosa succede dopo | prima del «sì»: «ELA riprende il task adesso; uno step affidato a un altro nodo aspetta»; dopo: **l'esito vero** del `run` (dec. H) | il `run` | — |

La durata è vera **con H1**: il grant lo conia l'executor al `run` che segue il «sì», con l'ora di
quel `run` (`permissions/authorizations.py:185-189`); con H1 i due istanti coincidono.

**`ApprovalOut` non basta.** Mancano il rischio, la sensibilità, i termini del grant, e le due parti
della domanda che la pagina deve mostrare separate. Proposta: entrano come campi — `description`
(la descrizione della capability nel catalogo), `risk`, `max_privacy`, `goal` (lo scopo dello step),
`stated` (gli argomenti di `prompt_arguments`, gli stessi che la frase porta già), `grant_uses`,
`grant_seconds` —, composti dove oggi si compone la
domanda, perché la CLI e la pagina li leggano dalla stessa rotta e la pagina non ricomponga niente
(la regola di dec. D). Nessun contenuto nuovo esce: scopo e argomenti dichiarati stanno già in
`prompt`, e `prompt` è già nell'audit (`APPROVAL_REQUESTED`).

**Il dispositivo non si mostra**, e la composizione di M17.1 («Device · MacBook») mostra una cosa che
ELA non può affermare: la domanda nomina **il livello e non il nodo**, perché «il livello è
immutabile, il piazzamento no» (ADR 0038 §16; `_may_travel`, `executive/executor.py:524-536`).

**F.2 — Il contenuto di un task `LOCAL_ONLY` sull'iPhone.** `PrivacyLevel` dice di sé «What data may
leave a node» (`domain.py:618-623`), e `LOCAL_ONLY` è il default di ogni task. Mostrare lo scopo di
uno step di un task `LOCAL_ONLY` sull'iPhone vuol dire farlo uscire dal Mac.

| | Regola | Il costo |
|---|---|---|
| **F2-a** | **il tetto del companion decide che cosa vede**: il confronto del filtro F2 dell'orchestratore (`devices/orchestrator.py:343`), `PRIVACY_ORDER[companion.privacy] <= PRIVACY_ORDER[task.max_privacy]`, con la `privacy` che porta l'`Identity` (dec. D) e il `max_privacy` che porta `ApprovalOut` o `TaskOut`. Per un task che non lo ammette la pagina **non porta** lo scopo del task (`TaskOut.goal`) né quello dello step, i `targets`, la frase della domanda (`prompt`), gli argomenti dichiarati: mostra l'id, lo stato, la capability, il rischio e le ore, e dice «il contenuto resta sul Mac: rispondi da lì». Il «sì» da lì è rifiutato — una sottoclasse nuova di `ApprovalNotAnswerableError`, accanto a quelle che dicono «già risposta» e «troppo tardi» (`ports.py:384-389`), `409` `not_answerable` —, perché non si approva ciò che non si vede (§30) | **per rispondere dall'iPhone un task va creato `TRUSTED`**, che vuol dire anche che può girare sul PC (dec. H) |
| F2-b | l'iPhone è lo schermo dell'utente, e mostrare non è mandare: vede tutto | legge `LOCAL_ONLY` come «dove si esegue» e non come «dove va il dato», contro il docstring |

**Raccomandazione: F2-a.** Usa un confronto che esiste, non un concetto nuovo; e un telefono rubato
legge solo ciò che l'utente ha dichiarato adatto a uscire dal Mac. Il campanello suona comunque —
non porta contenuto —, e la pagina dice perché la risposta va data al Mac. È la domanda 9.

**F.3 — Fino a quale rischio risponde l'iPhone: nessun tetto, e un test che se ne accorge.** Oggi una
domanda nasce solo per `MEDIUM`, o per `SAFE`/`LOW` con `requires_authorization`: `HIGH` e
`CRITICAL` sono negati prima di chiedere (`RISK_POLICY`). Un tetto «fino a `MEDIUM`» sarebbe un
rifiuto che nessun percorso di produzione può far scattare — ADR 0026 §7. Al suo posto, **un test
appunta che `RISK_POLICY` nega `HIGH` e `CRITICAL`**, e fallisce il giorno in cui la Fase 13 lo
cambia, con un messaggio che nomina questa domanda: è il congegno di ADR 0035 §7, e chi aprirà la
prima capability `HIGH` deciderà se l'iPhone può rispondere, e con quale conferma (§14 del design:
«Per azioni critiche … una conferma inequivocabile»).

**F.4 — La voce, detta da lontano.** M11.1 ha tenuto il testo di `voice.speak` fuori dalla domanda
perché «you hear the words a second later» (`permissions/capabilities.py:486-491`). Dall'iPhone non
è vero: il Mac parla in una stanza dove l'utente non è. La pagina lo dice per `voice.speak` e
`voice.speak_online` — «ELA parlerà ad alta voce su una macchina tua; le parole non sono nella
domanda» — e M11.1 dec. B non si riapre qui. Dichiarato.

### G. La chiusura della Fase 12 — *il censimento*

Ogni rimando del repository alla Fase 12 e a M12.5, con la sua sorte. Le citazioni esatte sono nel
materiale di lavoro del censimento; qui ci sono il posto e la risposta.

**Onorati qui:**

| Rimando | Che cosa chiede | Come |
|---|---|---|
| ADR 0037 §4 (`:213-215`), M12.1 dec. C e fuori scope (`:1129-1130`) | il permesso di rispondere, dato a un nodo preciso | dec. A, il ruolo imposto |
| ADR 0037 §5 (`:244-248`, `:263-266`), M12.1 dec. D (`:637-640`), «Da riesaminare» 2 (`:1405-1406`) | come il codice arriva, e la scadenza misurata dallo Shortcut | dec. C.4, P3 |
| ADR 0037 §7 (`:300`), vincolo dichiarato (`:896-897`), M12.1 dec. F (`:677-681`) e domanda 4 (`:1468-1471`), M12.2 (`:1256-1257`) | dove lo Shortcut tiene il segreto, e se condividerlo lo porta con sé | dec. C.2, P2 |
| ADR 0037 §15 (`:718-721`), vincolo dichiarato (`:901-903`); ADR 0023 §4 (`:110-113`); M8.1 (`:428-429`) | `responded_by` di un'identità autenticata che non è il Core | dec. A, C4 del contratto |
| ADR 0038 criterio di fine (`:44-46`), §18 (`:610-615`); M12.2 criterio di fine (`:96-99`), dec. P (`:1227-1238`), domanda 9 (`:1881-1883`), `:1258-1260`; le frasi della suite nominate in dec. A | M12.5 misurata contro la suite | **corretto apertamente**: il contratto del companion (dec. A) |
| M12.1 fuori scope (`:1126-1128`); M12.2 fuori scope (`:1344-1345`) | le notifiche push del companion | dec. E |
| ADR 0042 (`:24-27`, `:81-82`, `:437-438`, `:443-446`); `tests/design/test_no_network.py:5`; README del design system (`:97-104`); M17.1 (`:91`, `:1034-1035`, `:1200`) | collegare `tokens.css` e `components.css`; l'accesso autenticato dell'iPhone alle pagine | dec. D |
| `docs/STATO.md` 4.1, 5.1, 5.10 | il companion, con l'identità di M17.1; voce 17 di §34 del design | tutta la milestone; la 5.10 rivista (dec. D); le frasi a mano della 4.1 riscritte alla fine |
| `M17.3.md:34` | «il companion è M12.5» | tutta la milestone |
| ADR 0015 §1 (`:46-48`); `ports.py:951` | `pending` per l'iPhone | la pagina iniziale lo legge, attraverso la rotta |
| spec §15 («essere monitorata da iPhone»), §20 del design, voce 17 di §34 del design | vedere i task dall'iPhone | la pagina iniziale |
| M12.4 (`:45-50`); ADR 0040 §4 | un nodo dichiara solo ciò che sa fare | il companion dichiara di non saper fare niente, ed è vero |
| `apps/ios/README.md`, `nodes/ios/README.md` | i segnaposto di §48 | `apps/ios/` riceve le pagine; `nodes/ios/` dice che il companion non è un processo di nodo, e punta ad `apps/ios/` |

**Già onorati da una milestone chiusa** — nessuna azione: ADR 0016 §6 (la registrazione dall'esterno
è un evento: M12.1), ADR 0023 §7 e la 5.2 (fuori da loopback: M12.1), ADR 0025 (l'autenticazione per
nodo: M12.1), ADR 0026 (la scadenza sta sull'assegnazione: M12.2), ADR 0035 §5 (la revisione: M12.1),
ADR 0009 e M3.2 (una consegna dopo la chiusura: M12.2, ADR 0039 §7), M6.1, M8.3.

**Non si applicano al companion, con la ragione** — nessuna azione, e ADR 0043 lo scrive: `If-Match`
ed `ETag` (ADR 0037 §9) — il companion non annuncia; `performance` e `power_source` (ADR 0037 §10,
M12.3c) — non riporta; la rilettura della revisione (M12.3 dec. L) e la specie `residente` della CLI
(M12.3 dec. M) — nessun processo; ADR 0039 §7, «quando il Core non risponde» — Safari mostra la sua
pagina d'errore, e lo Shortcut il suo; ADR 0031 (`:45`, `:232`), «ogni nodo nuovo moltiplica le
scelte di piattaforma» — il companion non ha codice di piattaforma, e la regola resta in piedi per
chi ne avrà; i cinque minuti della decisione (ADR 0011) — al companion non arriva nessuna decisione;
la creazione idempotente di un task dall'iPhone (ADR 0008, M3.1) — creare task è fuori scope.

**Spostati, con la casa nuova — da decidere** (domanda 11). La Fase 12 si chiude e questi restano:

| Rimando | Che cosa resta | Casa proposta |
|---|---|---|
| ADR 0029 §16, ADR 0039 §6, `docs/STATO.md` 4.1, M10.2 (`:514`, `:556`, `:697`), M17.3 (`:54-57`) | `launchd` e l'eseguibile firmato di ELA | **una milestone sua, prima di M17.3**, che oggi dice «Nessuna milestone costruisce l'eseguibile firmato» e ne ha bisogno |
| ADR 0017 (`:126`), M12.1 «Da riesaminare» 6, ADR 0040 (`:322-324`) | i pesi di §17, «da ritarare in Fase 12, con dati reali»: misurati, non ritarati | **la Fase 13**, quando più capability viaggeranno e due nodi competeranno davvero |
| M12.2 «Da riesaminare» 6 | la deriva dell'orologio di un nodo contro i cinque minuti | la stessa milestone di §17 |
| M6.2, M6.3 | spostare un lavoro già in corso da un nodo a un altro (§12 del design) | la stessa |
| spec §22 (`:600`) | l'iPhone che conosce il contesto di una conversazione avuta al Mac | **la Fase 15** (la memoria) |
| spec §7, ADR 0042 §5 | i livelli di attenzione, la telefonata | **la Fase 15**, già scritto in ADR 0042 |

### H. Il «sì» che non fa ripartire il task — *nata dalla ricognizione*

`approve` porta il task a `QUEUED` (`tasks/engine.py:624-653`), e poi niente lo esegue finché
qualcuno non chiama `POST /tasks/{id}/run`: al Mac, con `ela task run`. ADR 0038 l'ha già dichiarato
per i piani remoti — «il ciclo che richiama `run` è il Proactive Core» (vincoli dichiarati). Un «sì»
dall'iPhone che resta lì fino al ritorno a casa non rende ELA usabile lontano dal Mac.

| | Che cosa fa il «sì» della pagina | Il costo |
|---|---|---|
| **H1** | **risponde, e poi esegue il task nella stessa richiesta**: le stesse due cose che l'utente fa al Mac, `ela task approve` e `ela task run`, ciascuna dalla sua funzione — il `run` con il suo lock, la sua attesa e il suo esito —; la pagina poi mostra dove si è fermato | la risposta della pagina aspetta quanto lo step; la CLI non cambia |
| H2 | un pulsante «prosegui» a parte, per ogni task `QUEUED` | un'azione in più per l'utente, e una rotta che non risponde a nessuna domanda: più vicina a «comandare» |
| H3 | niente: il task aspetta il Mac, dichiarato | il companion approva ma non fa lavorare |

**Raccomandazione: H1.** Non apre una rotta nuova per chi ruba il telefono: esegue solo ciò che
l'utente ha già creato e a cui ha appena detto sì, e ogni step `MEDIUM` successivo chiede di nuovo
(un grant è un uso solo). Un «no» non esegue niente: il task è `DENIED`.

**Il caso che H1 non copre, detto.** F.2 vuole i task a cui si risponde dall'iPhone dichiarati
`TRUSTED`, e un task `TRUSTED` può essere piazzato sul PC: ADR 0040 lo scrive per la voce, «con il
Mac a batteria ELA parla dal PC». Allora il `run` torna `ASSIGNED` (ADR 0038 §10), e la consegna del
PC chiude lo step **senza** proseguire il piano (ADR 0038, vincoli dichiarati: «il ciclo che
richiama `run` è il Proactive Core»). La pagina mostra l'esito vero — «affidato al PC, fino alle …»
— e non «fatto»; gli step dopo quello aspettano un `run`, dal Mac o dalla Fase 15. Il criterio di
fine vale quindi per uno step che gira su `local`, ed è così che la prova a mano lo costruisce: con
ELA isolata, senza il PC. È la domanda 3, perché tocca il punto fermo 3.

### I. Fermare un task da lontano (§65) — *proposta, come l'utente ha chiesto*

La rotta c'è (`POST /tasks/{id}/cancel`), firmata dall'identità risolta; per il companion è
un'aggiunta a `COMPANION_ROUTES`, un pulsante e una conferma. **Toglie soltanto**: un task
`CANCELLED` non fa più niente, e fermarsi è il verso di §33. Il prezzo: è irreversibile, e chi ruba
il telefono può fermare i task dell'utente — non leggerne di più, non farne di nuovi. Lo Shortcut
(`GLANCE`) non la raggiunge.

**Raccomandazione: sì, con una conferma in due tocchi**, e `TASK_CANCELLED` firmato `USER` con l'id
dell'iPhone. §65 vuole ELA «estremamente governabile», e fra i verbi di CONTROL di §3 del design c'è
«fermare task»; una rotta che toglie potere è la prima che ha senso dare a un telefono. Vale anche
per un task `LOCAL_ONLY` di cui la pagina non mostra il contenuto (F.2): fermare non chiede di
vedere. È la domanda 10.

## In scope

- **Il ruolo imposto** (dec. A): `DeviceRole` (`WORKER`, `COMPANION`, `GLANCE`) in `ela.domain`,
  sulla riga e sul codice; la migrazione `0011` con il default `WORKER`; `ela node enroll --role`;
  `POST /nodes/enrollments` che lo accetta; un corpo del nodo che lo porta riceve 422; la regola 44
  estesa.
- **Il companion nel middleware** (dec. A, C): due identità nuove, i loro elenchi di rotte letterali,
  il cookie come portatore del solo `COMPANION`, il 401 del prefisso con il cookie cancellato, il
  campo del codice sulla sola rotta di arruolamento della pagina, il motivo anonimo
  `core_on_a_companion_route`, l'attore `USER` con l'id del companion.
- **L'arruolamento, codice per rotta** (dec. C.5), con i rifiuti che non consumano il codice.
- **L'ultimo contatto e la disponibilità** (dec. B): il registro scrive `last_seen_at` a ogni
  richiesta autenticata di un companion o di uno Shortcut; la disponibilità derivata di chi non è
  `WORKER` è `UNAVAILABLE`; `DeviceOut` porta `role`; `ela device list` lo mostra.
- **Le pagine** (dec. D, F): la composizione HTML in `ela.api`, i modelli in `apps/ios/`, la CSP, i
  due fogli di stile di M17.1 sotto il prefisso, la sfera inclusa, la proiezione della presenza, gli
  errori come pagine; le regole in `tests/ios/`.
- **`ApprovalOut` estesa** (dec. F): `description`, `risk`, `max_privacy`, `goal`, `stated`,
  `grant_uses`, `grant_seconds`; la CLI che li mostra in `ela approvals`; l'`Identity` risolta che
  porta ruolo e `privacy`.
- **Il tetto del companion sul contenuto** (dec. F.2, se F2-a), con la terza sottoclasse del rifiuto.
- **Il «sì» che prosegue** (dec. H, se H1) e **fermare** (dec. I, se entra).
- **Il campanello** (dec. E): il port, il vocabolario chiuso, l'adattatore del fornitore scelto, le
  due variabili, `BELL_RUNG`, l'executor che suona.
- **Lo Shortcut**: la guida per costruirlo a mano, in `apps/ios/`, con i passi che P2 avrà
  verificato.
- **Il contratto del companion** (dec. A) in `tests/conformance/`, e le frasi della suite dei nodi
  corrette.
- **ADR 0043**, «Proposta» fino alla prova a mano, «Accettata» dopo.
- **La chiusura della Fase 12** (dec. G): le frasi a mano di `docs/STATO.md` 4.1, la 5.10 rivista,
  `M17.2.md` se la domanda 8 lo consente, la nota in `M12.2.md`, il changelog, `STATO.md` e
  `ARCHITECTURE.md` rigenerati.

## Out of scope

- **Creare task dall'iPhone, parlare a ELA, il campo «Ask ELA» e il pulsante «Talk» della
  composizione** (punto fermo 3). Ogni rotta che l'identità dell'iPhone raggiunge è una porta per chi
  ruba il telefono, e la prima milestone apre solo quella che rende ELA usabile lontano dal Mac.
  Creare un task è dare un ordine nuovo; parlare vuole il microfono di un altro dispositivo, che non
  è nessuno dei tre stati di §11.
- **Il controllo remoto** (§6, §20 del design) oltre a fermare (dec. I): mettere in pausa, cambiare
  priorità o dispositivo, modificare autorizzazioni, far proseguire un task senza una domanda. Sono
  CONTROL di §3 del design, e aggiungono potere invece di toglierlo.
- **I livelli di attenzione e la telefonata** (§7, §13 del design): la Fase 15 (ADR 0042 §5, punto
  fermo 6). La priorità del fornitore è una costante.
- **Decidere se suonare** (§8, §34): suona ogni approvazione, anche con l'utente al Mac. È della Fase
  15, e il vincolo è dichiarato.
- **App nativa, App Intents, deep link** (§6): la 5.1.
- **Web Push**: vuole una web app della schermata Home, JavaScript per l'iscrizione e un contesto
  sicuro, che `http://100.x` non è — tutte e tre escluse da dec. D.
- **La pagina aggiunta alla schermata Home**: il tocco apre Safari (dec. C).
- **HTTPS sulla tailnet** (dec. D).
- **Il tema chiaro e il tema del sistema** (dec. D).
- **Le viste di M17.2**: il Device Center, l'Approval Center da scrivania, la Task Intelligence View.
  Il companion non ha una pagina dei nodi.
- **Un'identità del Mac per il browser**: il Command Center sul Mac è di M17.2.
- **Il contesto condiviso fra Mac e iPhone** (§22): la Fase 15.
- **Riaprire le decisioni ereditate**: nessuna.

## Criteri di accettazione

Ognuno dice il comando che lo prova e che cosa lo fa fallire.

1. **Il contratto del companion passa con i due portatori** — le storie di dec. A, ciascuna con i
   portatori che la sua riga nomina. *Fallisce* `tests/conformance/test_companion_contract.py` su
   qualunque storia, e un test che appunta l'elenco delle storie e le dichiarazioni per ruolo se ne
   sparisce una o se ne cresce una in silenzio.
2. **Ogni identità raggiunge solo le sue rotte, dal suo portatore.** La matrice si **deriva** da
   `app.routes`, come quella di M12.1: una rotta aggiunta domani è coperta per costruzione.
   *Fallisce* `tests/api/test_security.py` se una rotta fuori dall'elenco del suo ruolo risponde a un
   companion o a uno Shortcut, se una rotta del companion risponde a un nodo, se il segreto di un
   ruolo passa dal portatore dell'altro, o se il token del Core ne raggiunge una: il Core non è un
   companion, come non è un nodo (ADR 0037 §4), e una pagina che legge il tetto del companion (dec.
   F.2) con il token del Core non avrebbe un tetto da leggere.
3. **Il 401 del companion segue le regole di dec. C.3, nel loro ordine**: `POST /companion/enroll`
   risolve solo il campo del codice, anche con un cookie revocato o un header presenti; il rifiuto di
   chi presenta un header è il JSON di oggi, e il token del Core è contato come
   `core_on_a_companion_route`; senza header sotto `/companion/`, una credenziale che fallisce riceve
   la pagina con il cookie cancellato, e una buona sulla rotta sbagliata la stessa pagina con il
   cookie intatto. *Fallisce* `tests/api/test_companion.py`, un caso per regola, uno che arruola con
   un cookie revocato presente, e uno che confronta byte per byte due 401 della pagina dati a chi non
   ha una credenziale valida, per ragioni diverse.
4. **Il ruolo non lo scrive il nodo, e il codice decide la rotta.** Un arruolamento, un annuncio o un
   heartbeat che porta `role` riceve 422 e la riga non cambia; ogni riga della tabella di C.5 ha il
   suo esito, e un codice rifiutato resta spendibile. *Fallisce* `tests/api/test_nodes.py` e
   `tests/api/test_companion.py`.
5. **Chi sceglie scrive.** Con il «sì» della pagina e il `run` che lo segue, `responded_by` e l'id
   dell'attore di `APPROVAL_RESOLVED` e di `AUTHORIZATION_GRANTED` sono l'id del companion: *fallisce*
   se sono quello di `local`. Se dec. I entra, `TASK_CANCELLED` è `USER` con l'id del companion: *fallisce*
   con `DEVICE` o con l'id di `local`. `tests/api/test_companion.py`.
6. **Una porta sola per il «sì», e le pagine leggono le rotte.** La regola 19 resta senza esenzioni
   nuove; la regola nuova di dec. D fallisce se `api/companion.py` nomina un port, uno store, il
   catalogo o l'executor attraverso `Ela`, con il suo caso negativo in `violations.py`.
7. **Il segreto non si vede.** Il cookie si imposta solo nella risposta dell'arruolamento della
   pagina, `HttpOnly`, `SameSite=Strict`, `Path=/companion`; il suo valore non compare in un corpo,
   in un URL, nell'audit. *Fallisce* la regola 46 estesa, e un test che legge ogni risposta del
   companion.
8. **Solo `api/security.py` legge la credenziale**, header o cookie: la regola 47 estesa, con il suo
   caso negativo.
9. **Niente JavaScript.** Nessun modello di `apps/ios/` contiene `<script>`, `on*` o `javascript:`,
   e ogni risposta HTML di `ela.api`, il 401 della pagina compreso, porta la CSP. *Falliscono*
   `tests/ios/` e un test che legge le intestazioni di ogni pagina.
10. **Nessun valore crudo.** Uno scopo di step con `<script>` e `"` arriva escapato su ogni pagina che
    lo mostra. *Fallisce* il test dell'escape.
11. **Le pagine sono fatte di componenti.** Ogni classe di un modello è definita in `components.css`;
    la sfera viene da `_orb.html`. *Fallisce* `tests/ios/`, che riusa il controllo delle composizioni
    di `tests/design/`.
12. **La presenza è una proiezione**: il codominio della funzione è dentro le chiavi di `tokens.json`,
    la precedenza è quella di dec. D, e nessuno stato si memorizza. *Fallisce* il test della
    proiezione, e quello di M17.1 che guarda `domain.py`.
13. **Gli errori sotto il prefisso sono pagine**, con lo stato dell'errore e la CSP. *Fallisce*
    `tests/api/test_companion.py`, un caso per riga di `FAILURES` che una pagina può incontrare.
14. **Il campanello non porta lettere.** Il port accetta solo il vocabolario; ogni voce ha uno
    scrittore; ciò che il fornitore finto riceve per un task con scopo e argomenti riconoscibili è
    una voce del vocabolario; l'argomento non compare nell'audit né in un URL. *Fallisce* il test
    nella forma di `test_wire_codes.py`, con i suoi negativi.
15. **Un campanello che non suona non ferma niente**: il fornitore finto che fallisce lascia la
    domanda in attesa, e `BELL_RUNG` con l'esito. *Fallisce* `tests/executive/`.
16. **Il tetto del companion** (se F2-a): per un task `LOCAL_ONLY` nessuna pagina del companion porta
    i campi di dec. F.2, e il «sì» da lì è rifiutato con la sottoclasse nuova. *Fallisce* C5.
17. **Il test che si accorge del primo `HIGH`** (F.3): fallisce se `RISK_POLICY` smette di negare
    `HIGH` o `CRITICAL`, e il messaggio nomina la domanda del companion.
18. **Un companion non è mai disponibile né idoneo, e il suo contatto si vede**: `available: false`
    sempre, la disponibilità derivata `UNAVAILABLE` anche subito dopo una sua richiesta, nessun task
    né step aperto a suo nome nemmeno per uno step senza capability, `last_seen_at` dopo ogni sua
    richiesta, `role` in `GET /devices`. *Falliscono* `tests/devices/` e C3.
19. **Ogni rotta nuova è classificata** in `tests/docs/test_adr_cli.py`, per nome, come quelle dei
    nodi.
20. **ADR 0043 esiste, e i suoi numeri sono appuntati**: *fallisce* un `tests/docs/test_adr_companion.py`
    nella forma di `tests/docs/test_adr_nodes_windows.py`, e il test delle rotte degli ADR
    (`tests/docs/test_adr_composition.py::test_the_routes_of_the_adrs_are_the_routes_of_the_code`) se
    la tabella delle rotte di ADR 0043 non è quella del codice.
21. **La prova a mano** (§«La prova a mano») è passata sull'iPhone, con i suoi esiti scritti qui.
22. **`make check` verde una volta, alla fine; la CI verde sui due runner.**

## Test previsti

- `tests/conformance/test_companion_contract.py`, con un driver per portatore: le storie di dec. A.
- `tests/api/test_companion.py`: le pagine, la risposta, l'arruolamento dal modulo, le regole
  del 401, `Origin`, la CSP, l'escape, gli errori come pagine, la tabella di C.5.
- `tests/api/test_security.py`: la matrice delle identità e dei portatori.
- `tests/api/test_approvals.py`: `ApprovalOut` estesa.
- `tests/devices/`: la disponibilità derivata per ruolo, `available()`, l'ultimo contatto.
- `tests/executive/`: il campanello nell'executor, il campanello che fallisce.
- `tests/providers/<fornitore>/`: l'adattatore, contro un server finto.
- `tests/ios/`: niente script, solo componenti, titoli senza contenuto, la guida dello Shortcut che
  nomina le rotte vere.
- `tests/architecture/`: le regole 46 e 47 estese, e le nuove, ciascuna con il suo negativo in
  `violations.py`.
- `tests/docs/`: `test_adr_companion.py`, i conteggi, la continuazione di M9.4.
- Contract test del port nuovo, sul finto e sull'adattatore.
- Un test del middleware che legge il campo `code` e lascia il corpo alla rotta (§«Rischi»), scritto
  prima del codice di C.3.

## Le liste che questa milestone muove

Ciò che ogni aggiunta costringe a rileggere — i totali li appuntano i test, non questo documento:
`RULES` e `docs/STATO.md` §3; `REQUIRED_PORTS`; `AuditEventType` e il test «ogni tipo di evento ha
uno scrittore»; `Anonymous` in `api/security.py` e i contatori di `/diagnostics`; le eccezioni di
`ports.py` e `FAILURES`; le rotte di `ela.api`, `create_app` e `tests/api/test_security.py`; la
classificazione delle rotte in `tests/docs/test_adr_cli.py`; la tabella delle rotte degli ADR letta
da `tests/docs/test_adr_composition.py`; `.env.example` e `VARIABLES` di `cli/setup.py`; le
migrazioni; `DeviceOut` e `ApprovalOut`; `KITS` e le frasi della suite; `CONTINUATIONS` in
`tests/docs/test_simplifications.py`, con la sezione «L'elenco di ciò che è semplificato — la
continuazione di M9.4» di questo documento; `docs/adr/README.md`; `apps/README.md` e il README di
`apps/ios/`; `docs/GETTING_STARTED.md`; `docs/ARCHITECTURE.md` rigenerato; `docs/STATO.md` rigenerato e
le sue frasi a mano.

## Regole di architettura

Ognuna nel commit **prima** del codice che difende, con il suo caso negativo (ADR 0030 §15):

| Regola | Che cosa dice |
|---|---|
| 46 estesa | il valore del cookie e l'argomento del campanello sono segreti: fuori da ogni `AuditEvent`, da ogni forma di `api/schemas.py`; il `Set-Cookie` con la credenziale si compone in un posto solo |
| 47 estesa | nessun modulo di `ela.api` fuori da `api/security.py` legge il cookie del companion |
| nuova: **le pagine leggono le rotte** | `api/companion.py` chiama le funzioni delle rotte di `ela.api` e non nomina un port, uno store, il catalogo o l'executor attraverso `Ela` |
| nuova: **un campanello suona un codice** | il port del campanello ha un solo chiamante, l'executor, e nessun parametro di tipo `str` |
| nuova: **una pagina passa da un compositore solo** | nessun modulo di `ela.api` costruisce una risposta HTML fuori dal compositore che mette la CSP; il 401 della pagina compreso |

Le regole di `apps/ios/` — niente script, solo componenti, titoli senza contenuto — non camminano
Python: vivono in `tests/ios/`, e il README della cartella le nomina con i loro test.

## Rischi

- **Leggere un campo del modulo nel middleware.** Misurato il 2026-09-19 con le versioni del
  repository (FastAPI 0.141.1, Starlette 1.6.0), in un'app usa-e-getta fuori dall'albero: un
  middleware `http` legge `code` dal corpo di un `POST` con un modulo, e la rotta rilegge lo stesso
  corpo intero. Resta un rischio perché un aggiornamento di Starlette può cambiarlo: **un test lo
  tiene**, scritto prima del codice di C.3.
- **Il `run` dentro la risposta della pagina** (H1): uno step lungo tiene Safari in attesa; se la
  pagina si chiude a metà, il `run` continua, come per la CLI.
- **Due identità per un telefono** (C.2): revocare l'iPhone vuol dire revocarne due. `ela device list`
  le mostra una sotto l'altra, con il ruolo e l'ultimo contatto.
- **La cronologia di Safari**: anche un id opaco racconta *quando* l'utente ha guardato ELA.
- **I companion in ogni `DEVICE_SELECTED`** come candidati scartati: rumore vero.
- **Il doppio clic sul codice** (C.4): se P3 dice che taglia il codice, l'uscita di `ela node enroll`
  cambia per i ruoli del companion.

## Vincoli dichiarati previsti per ADR 0043

- **Il companion non prende lavoro, e non recita la suite dei nodi**: si misura contro il suo
  contratto (dec. A).
- **L'ultimo contatto è la parola del registro, e un companion non è mai disponibile** (dec. B).
- **Un telefono, due identità**: Safari e lo Shortcut si arruolano ciascuno per sé, con ruoli
  diversi (dec. A, C.2).
- **Il cookie porta la credenziale durevole**, senza rotazione come il segreto di un nodo, per 400
  giorni; `Secure` no, perché è `http` (dec. C.1).
- **Che cosa porti con sé un backup iCloud dell'iPhone non è stabilito**, né per il file dello
  Shortcut né per i cookie di Safari (dec. C.2).
- **Il prefisso `/companion/` si vede da chi bussa senza credenziali**: ADR 0023 §7 rivisto (dec. C.3).
- **Un codice `WORKER` speso dallo Shortcut senza `os` `IOS` fa nascere un nodo muto** (dec. C.5).
- **http sulla tailnet**: la cifratura è di WireGuard (dec. D).
- **La pagina di arruolamento è senza identità visiva**, con il carattere di default di Safari
  (dec. D).
- **Le pagine del companion sono nel tema scuro** (dec. D).
- **ELA gira dal repository**: il percorso di `apps/` si deriva dal package (dec. D).
- **La presenza mostra tre stati**, e gli altri aspettano M17.2 (dec. D).
- **Il campanello suona ogni approvazione, anche con l'utente al Mac** (dec. E).
- **Con E1, chi conosce l'argomento legge i campanelli e ne suona di falsi**, e il testo — senza
  niente dell'utente — e l'indirizzo del Mac sulla tailnet passano in chiaro da ntfy.sh, da FCM e da
  APNs (dec. E).
- **Una morte fra la domanda e il campanello lascia una domanda senza campanello** (dec. E).
- **Il dispositivo, le conseguenze e la reversibilità non si nominano in una domanda** (dec. F).
- **Nessun tetto di rischio per il companion finché nessuna domanda supera `MEDIUM`** (dec. F.3).
- **Dall'iPhone, la voce si approva senza sentire** (dec. F.4).
- **Un «sì» dall'iPhone prosegue il task finché gli step restano su `local`**; uno step affidato a un
  altro nodo aspetta un `run` (dec. H).

## La prova a mano

Due macchine vere: il Core su questo Mac, l'iPhone sulla tailnet. **Senza chiave di Anthropic.** I
passi esatti si scrivono dopo le decisioni, perché dipendono da E e da H; la forma è questa:

1. ELA isolata in una cartella temporanea (`ELA_DB_URL`, `ELA_WORKSPACE_DIR`, porta diversa), con
   `ELA_API_TAILNET_HOST` e il campanello configurato; nessun altro nodo, così ogni step gira su
   `local` (dec. H).
2. Safari sull'iPhone: la pagina chiede un codice; `ela node enroll --role companion --privacy
   TRUSTED` al Mac; il codice incollato; la pagina iniziale con la sfera `IDLE`. Lo stesso per lo
   Shortcut, con `--role glance --privacy TRUSTED`.
3. Un task `TRUSTED` con un piano a mano di due step — `core.echo` con `requires_authorization`, poi
   `voice.speak` — e `ela task run`: il telefono suona; il testo è quello del vocabolario.
4. Il tocco apre la pagina; la domanda nomina capability, scopo, rischio, sensibilità, durata; «sì»;
   la pagina torna con l'esito; il telefono suona per il secondo step; «sì»; **il Mac parla**.
5. `ela audit tail` al Mac: `responded_by` e gli attori portano l'id dell'iPhone.
6. Un task `LOCAL_ONLY`: la pagina non porta lo scopo, e dice di rispondere dal Mac (F2-a).
7. Lo Shortcut, da Siri: dice quante cose aspettano.
8. `ela node revoke <iPhone>`: la pagina torna a chiedere un codice e il cookie sparisce, lo Shortcut
   riceve 401, e `DEVICE_REJECTED` `revoked` è nell'audit; un nuovo codice riarruola Safari.
9. Pulizia: la cartella temporanea, i processi, le identità revocate, l'argomento di prova.

## La rilettura, e che cosa ha cambiato

Una rilettura critica, fatta da un agente separato sulla prima stesura completa, il 2026-09-19. Ogni
rilievo è verificato sul codice prima di toccare il testo, e un secondo giro ha riletto la versione
corretta. Le citazioni che la rilettura ha verificato giuste non si ripetono qui. Ciò che ha cambiato
la spec:

- **Le rotte non distinguevano il portatore**, e lo Shortcut, con un segreto in un file, avrebbe
  potuto rispondere e fermare. Ora i ruoli sono uno per custodia — `COMPANION` per Safari, `GLANCE`
  per lo Shortcut —, e il portatore sbagliato è un 401 (dec. A, C.2).
- **Un companion poteva essere idoneo.** Uno step senza capability non fa scattare `MISSING_TOOL`
  (`devices/orchestrator.py:347-348`), e con una disponibilità derivata dall'ultimo contatto il runner
  avrebbe aperto task e step a nome dell'iPhone (`executive/runner.py:265-269`). Verificato sul
  codice; ora la disponibilità di chi non è `WORKER` è `UNAVAILABLE` per costruzione (dec. B).
- **Il 401 del companion non era definito**, e cambiava ADR 0023 §7 senza dirlo; un cookie revocato
  avrebbe bloccato il riarruolamento. Ora ci sono le regole di C.3, e la revisione è detta. Il
  secondo giro ha trovato che nel loro primo ordine l'arruolamento di Safari non arrivava mai alla
  sua regola, e che un cookie ancora buono si sarebbe cancellato su un percorso non suo: corretti.
- **L'arruolamento non diceva quale codice vale su quale rotta**, né chi scrive `name` e `os`: C.5.
- **H1 non copriva uno step affidato al PC**, e la pagina avrebbe detto «prosegue» dove il task
  aspetta: dec. H lo dice, e la pagina mostra l'esito vero.
- **ADR 0037 §2 ha una seconda voce che il companion fa cadere** — «un «sì» entra da una porta
  sola» —: punto fermo 2, riletto.
- **«Mai un secondo accesso al database» non aveva una difesa**, e la pagina leggeva un port e
  ricomponeva la domanda: la regola «le pagine leggono le rotte», e `ApprovalOut` con `goal` e
  `stated` (dec. D, F). Il secondo giro ha trovato che la regola lasciava fuori la descrizione del
  catalogo e il tetto del companion: la descrizione entra in `ApprovalOut`, e il tetto lo porta
  l'`Identity` che il middleware risolve.
- **Un criterio registrato di M17.2 cambia con D1**, e la stesura taceva: domanda 8.
- **Un'asserzione che non poteva fallire**: l'attore `USER` di `APPROVAL_RESOLVED` è per costruzione;
  il negativo vero è sull'id, e `DEVICE` si prova su `TASK_CANCELLED` (criterio 5).
- E i minori: le due voci di §14 del design che mancavano, la proiezione detta come scelta con la sua
  precedenza, gli errori come pagine, il tema, il carattere della pagina di arruolamento, il tocco
  dall'indirizzo su cui ELA ascolta davvero, le variabili del campanello, l'elenco dei campi nascosti
  da F.2, i luoghi delle frasi della suite al posto di un loro conteggio, il censimento completato, le
  liste e i test che la milestone muove, e nell'appendice un passo che non distingueva niente (P1,
  passo 8), una misura che non seguiva il gesto vero (P3), un `tee` che troncava (P4) e una sonda che
  scriveva nel log il valore che diceva di non scrivere.

## Da portare all'utente

**Nessuna decisione ereditata va riaperta**, e nessun punto fermo è smentito da un fatto. Le
tensioni trovate sono scritte invece di risolte: la più grossa è che **un «sì» oggi non fa ripartire
il task** (domanda 3), e la più delicata tocca un criterio già registrato di M17.2 (domanda 8).

| # | Domanda | Raccomandazione | Si chiude |
|---|---|---|---|
| 1 | **L'iPhone è un nodo?** (dec. A) | **no**: una riga del registro con un ruolo imposto — `COMPANION` per Safari, `GLANCE` per lo Shortcut — che decide portatore e rotte, e non prende lavoro; il criterio di fine di M12.2 si corregge apertamente, e M12.5 si misura contro il contratto del companion | adesso; P2 conferma che cosa recita lo Shortcut |
| 2 | **«Connesso»** (dec. B) | **B2**: l'ultimo contatto, scritto dal registro a ogni richiesta; la disponibilità di un companion è `UNAVAILABLE` per costruzione, e nessuno lo dice «connesso» | adesso |
| 3 | **Il «sì» dall'iPhone fa proseguire il task?** (dec. H) — tocca il punto fermo 3 | **H1**: la pagina risponde e poi esegue, come `approve` e `run` al Mac; uno step affidato al PC aspetta comunque un `run`, detto | adesso |
| 4 | **Safari: il cookie, il 401 del prefisso che è il modulo di arruolamento, e quale codice vale su quale rotta** (dec. C.1, C.3, C.5) — rivede ADR 0023 §7 e cambia `POST /nodes/enroll` | un cookie `HttpOnly; SameSite=Strict; Path=/companion` con la credenziale, cancellato quando la credenziale fallisce; `Origin` controllato; il prefisso si vede, le rotte no; un codice `COMPANION` o un `WORKER` con `os` `IOS` su `POST /nodes/enroll` → `422` | dopo **P0** e **P1** |
| 5 | **Lo Shortcut: un'identità `GLANCE`, il segreto in un file sul telefono, solo il riassunto** (dec. C.2) | sì, due righe per un telefono | dopo **P2** |
| 6 | **Il codice arriva con gli Appunti universali** (dec. C.4) | sì; e la scadenza di dieci minuti regge o no, e il doppio clic prende il codice intero o no | dopo **P3** |
| 7 | **Il campanello** (dec. E): il fornitore; una sola voce; nell'audit; non una capability; enum o metodo | **E1**, ntfy.sh gratis con un argomento casuale (E1+ a $60 all'anno toglie i campanelli falsi; E3, Pushover a $4.99, cifra ma apre la sua app); una sola voce, l'approvazione che aspetta; `BELL_RUNG` nell'audit; **non** una capability; l'enum di una voce | il fornitore dopo **P4** e la scelta di costo di Tommaso; il resto adesso |
| 8 | **Le pagine** (dec. D): le compone `ela.api` e leggono le sue rotte, http, niente JavaScript, tema scuro, `apps/ios/`; la 5.10 riscritta così, **e il criterio registrato di M17.2 «con il token, come la CLI» riscritto** | **D1** | adesso; P0 dice che cosa mostra Safari per `http` |
| 9 | **Il contenuto di un task `LOCAL_ONLY` sull'iPhone** (dec. F.2) | **F2-a**: il tetto del companion decide che cosa vede, e per rispondere dall'iPhone un task va creato `TRUSTED` | adesso |
| 10 | **Fermare un task dall'iPhone** (dec. I) | **sì**, con una conferma; lo Shortcut no | adesso |
| 11 | **Le case nuove di ciò che la Fase 12 lascia** (dec. G) | l'eseguibile firmato in una milestone sua prima di M17.3; i pesi di §17, la deriva degli orologi e il trasferimento del lavoro nella Fase 13; §22 e l'attenzione nella Fase 15 | adesso |
| 12 | **`ApprovalOut` e il rischio** (dec. F, F.3) | guadagna `description`, `risk`, `max_privacy`, `goal`, `stated`, `grant_uses`, `grant_seconds`; il dispositivo, le conseguenze e la reversibilità non si mostrano; nessun tetto di rischio per l'iPhone, e un test che si accorge del primo `HIGH` | adesso |
| 13 | **La presenza** (dec. D): tre stati — `WAITING APPROVAL`, `WORKING`, `IDLE` — in quest'ordine, e gli altri a M17.2 | sì | adesso |

## Appendice — le misure sull'iPhone

**Un blocco alla volta**, nell'ordine. Ogni blocco dice che cosa toccare e che cosa deve comparire.
Dove un passo non va come scritto, **ci si ferma e si riporta ciò che è comparso**: è una misura
anche quella. I nomi delle app e dei menu sono quelli di iOS in italiano, con l'inglese fra
parentesi.

**Che cosa cambia sull'iPhone, e come si torna indietro.** P1 aggiunge un'icona alla schermata Home;
P3 può chiedere di accendere Handoff, se è spento; P4 installa l'app ntfy, le dà il permesso delle
notifiche e accende per un momento «Non disturbare». Ogni blocco ha la sua pulizia, e la pulizia di
P4 spegne «Non disturbare»; Handoff e l'app si possono lasciare, o togliere, e va detto che cosa si è
fatto.

**Sul Mac, una volta per tutte le misure**, in un terminale:

```
cd /Users/tommasosilvi/ELA
git switch m12.5-iphone-companion && git pull
ip=$(tailscale ip -4 2>/dev/null || /Applications/Tailscale.app/Contents/MacOS/Tailscale ip -4)
echo "$ip"
```

Deve comparire l'indirizzo del Mac sulla tailnet, `100.` e altri tre numeri (a M17.1 era
`100.76.92.39`). È l'indirizzo che i blocchi chiamano `<ip>`: sull'iPhone va scritto per intero.
**Se si apre un terminale nuovo, le righe `cd …` e `ip=…` vanno rilanciate** — i comandi dei blocchi
partono dalla cartella del repository. L'ora nei file della sonda è quella del Mac, con il fuso
(`+0200`).

### P0 — l'iPhone e la tailnet

1. **iPhone** — Impostazioni (Settings) → Generali (General) → Info (About): annota **Versione iOS**.
2. **iPhone** — apri l'app **Tailscale**: in alto deve dire che è connesso, e il Mac deve comparire
   nell'elenco dei dispositivi.
3. **Mac** — nel terminale:
   ```
   ELA_API_TAILNET_HOST="$ip" uv run ela serve 2>&1 | tee ~/Downloads/ela-p0-mac.txt
   ```
   Deve partire senza errori e restare acceso.
4. **iPhone** — apri **Safari**, tocca la barra degli indirizzi, scrivi
   `http://<ip>:8351/health` e vai. Deve comparire una riga di testo che comincia con
   `{"error":{"code":"unauthorized"`: il Core risponde, e il middleware rifiuta una richiesta senza
   credenziale — è ciò che deve fare.
5. **iPhone** — guarda **prima** che compaia il testo: Safari ha mostrato una pagina d'avviso? Nella
   barra, accanto all'indirizzo, c'è una scritta come «Non sicuro» (Not Secure)? Poi in Impostazioni
   → App → Safari annota se **«Avviso connessione non sicura»** (Not Secure Connection Warning) è
   acceso. Fai uno **screenshot** (tasto laterale + volume su) della pagina con la barra visibile.
6. **Mac** — `Ctrl-C` nel terminale di `ela serve`.

**Da riportare:** `~/Downloads/ela-p0-mac.txt`; lo screenshot, mandato al Mac con AirDrop in
`~/Downloads` come `ela-p0-iphone.png`; la versione di iOS e le risposte del passo 5, scritte in
`~/Downloads/ela-p0-note.txt`.

### P1 — Safari e il cookie

Una **sonda** sul Mac: un server usa-e-getta, solo libreria standard, che non tocca ELA e ascolta
solo sull'indirizzo della tailnet. Scrive ogni richiesta in `~/Downloads/ela-probe.txt` e **non
scrive mai il valore** di un cookie, di un header o di un campo: solo se c'era, quanto era lungo e con
che cosa combaciava. I valori da copiare li stampa solo nel terminale. Il suo codice è in fondo a
questa appendice, e il primo comando lo estrae da qui.

1. **Mac** — nel terminale:
   ```
   sed -n '/^# ela_probe START/,/^# ela_probe END/p' docs/milestones/M12.5.md > ~/Downloads/ela_probe.py
   uv run --no-project python ~/Downloads/ela_probe.py "$ip"
   ```
   Deve comparire `--- avvio su http://<ip>:8172/`, e la sonda resta accesa. Stampa anche un codice
   e un header: in questo blocco non servono. Se invece dice «serve l'indirizzo del Mac sulla
   tailnet», la riga `ip=…` va rilanciata.
2. **iPhone** — Safari → `http://<ip>:8172/`. Deve comparire «ELA — sonda M12.5» e «Cookie in
   questa richiesta: **assente**».
3. **iPhone** — tocca **«1. Imposta il cookie»**. La pagina torna e deve dire **«len=43
   COMBACIA-1»**.
4. **iPhone** — trascina la pagina verso il basso per ricaricarla: deve dire ancora «COMBACIA-1».
5. **iPhone** — chiudi Safari davvero: scorri dal bordo basso verso l'alto e fermati a metà, poi
   spingi Safari verso l'alto finché sparisce. Riapri Safari e ricarica la pagina. Annota che cosa
   dice.
6. **iPhone** — riavvia l'iPhone (tasto laterale + volume, «scorri per spegnere», poi riaccendi).
   Apri **Tailscale** e aspetta che sia connesso. Safari → ricarica la pagina. Annota che cosa dice.
7. **iPhone** — in Safari, tocca il pulsante **Condividi** (il quadrato con la freccia) →
   **Aggiungi alla schermata Home** (Add to Home Screen). Prima di toccare **Aggiungi**, annota se
   c'è l'interruttore **«Apri come app web»** (Open as Web App) e se è acceso. Poi **Aggiungi**, e
   apri la nuova icona dalla schermata Home. Annota che cosa dice la pagina lì dentro: `assente` vuol
   dire cookie separati da Safari.
8. **iPhone** — **da lì dentro** tocca **«1-bis. Imposta un altro cookie»**: deve dire
   «COMBACIA-2». Poi torna in **Safari** (l'app, non l'icona) e ricarica la pagina. Annota che cosa
   dice: **«COMBACIA-1»** vuol dire che Safari e la pagina della schermata Home tengono i cookie
   separati; **«COMBACIA-2»** vuol dire che li tengono insieme.
9. **iPhone** — in Safari, nel campo **«2. Incolla qui il codice»**, scrivi `prova` e tocca
   **Invia**. La pagina torna com'era: il file del Mac registra se Safari ha mandato l'header
   `Origin`.
10. **iPhone** — apri una scheda di **Navigazione privata** (in Safari: il pulsante delle schede →
    «Privata») e vai a `http://<ip>:8172/`. Deve dire «assente». Chiudi la scheda privata.
11. **Mac** — `Ctrl-C` nel terminale della sonda.
12. **Pulizia** — sull'iPhone, tieni premuta l'icona della sonda nella schermata Home → **Elimina
    app** (o «Rimuovi segnalibro»). Il cookie della sonda resta in Safari e non vale niente: si può
    togliere da Impostazioni → App → Safari → Avanzate → Dati dei siti web, cercando l'indirizzo.

**Da riportare:** `~/Downloads/ela-probe.txt` — contiene già, riga per riga, se il cookie c'era e
quale, l'`Origin` e lo User-Agent, che dice anche la versione di iOS —, e le annotazioni dei passi
5–8 in `~/Downloads/ela-p1-note.txt`.

### P2 — lo Shortcut

Serve la sonda accesa: sul Mac, `uv run --no-project python ~/Downloads/ela_probe.py "$ip"`. Nel
terminale — e solo lì — compare una riga **«L'header per lo Shortcut:  Bearer probe.…»**: il valore
da scrivere è `Bearer probe.` seguito da undici caratteri, cioè tutto ciò che viene dopo i due
punti. È una credenziale finta della sonda, non di ELA.

**Uno Shortcut che parla con un header.**

1. **iPhone** — apri l'app **Comandi rapidi** (Shortcuts) → **+** in alto a destra. Tocca il nome in
   alto e chiamalo `ELA prova`.
2. Cerca e aggiungi l'azione **«Ottieni contenuto dell'URL»** (Get Contents of URL). Nel campo
   dell'URL scrivi `http://<ip>:8172/json`.
3. Tocca la freccia accanto all'azione (o **«Mostra altro»**): **Metodo** `GET`; **Intestazioni**
   (Headers) → **Aggiungi nuova intestazione**: chiave `Authorization`, valore `Bearer probe.…`
   (quello del terminale).
4. Aggiungi l'azione **«Mostra risultato»** (Show Result).
5. Tocca **▶** in basso. Se chiede il permesso di connettersi all'indirizzo, consenti e annota la
   frase esatta della domanda. Deve comparire `{"ok": true, "authorization": "len=17 COMBACIA"}`.
   Se invece compare un errore — `http` verso un indirizzo IP potrebbe essere vietato —, annota la
   frase esatta. Fai uno screenshot.

**Se il testo di uno Shortcut viaggia con iCloud.**

6. **iPhone** — nello stesso Shortcut aggiungi in cima un'azione **«Testo»** (Text) con dentro
   `FINTO-SEGRETO-M125`. Tocca **Fine**.
7. **iPhone** — Impostazioni → App → Comandi rapidi (su versioni meno recenti: Impostazioni →
   Comandi rapidi): annota se **«Sincronizzazione iCloud»** (iCloud Sync) è accesa.
8. **Mac** — apri l'app **Comandi rapidi**. Aspetta un minuto. Cerca `ELA prova`: annota se c'è e,
   se c'è, aprilo e annota se dentro si legge `FINTO-SEGRETO-M125`.

**Un segreto in un file sul telefono, che lo Shortcut rilegge.**

9. **iPhone** — un secondo Shortcut, `ELA salva`: azione **«Testo»** con `FINTO-SEGRETO-FILE`, poi
   azione **«Salva file»** (Save File). Tocca la freccia dell'azione: spegni **«Chiedi dove
   salvare»**, e come destinazione scegli una cartella **«Sul mio iPhone»** (On My iPhone) — non
   iCloud Drive; nome del file `ela-prova.txt`; accendi **«Sovrascrivi se il file esiste»**.
   Eseguilo con **▶**. Annota quale cartella hai potuto scegliere.
10. **iPhone** — un terzo Shortcut, `ELA leggi`: azione **«Ottieni file»** (Get File) — spegni
    «Mostra selettore documenti» e indica lo stesso percorso —, poi **«Mostra risultato»**.
    Eseguilo: deve comparire `FINTO-SEGRETO-FILE`.
11. **Mac** — apri **Comandi rapidi** e il **Finder** → iCloud Drive: annota se `ELA salva` e `ELA
    leggi` compaiono sul Mac, e se `ela-prova.txt` compare in iCloud Drive.
12. **Mac** — `Ctrl-C` nel terminale della sonda.
13. **Pulizia** — cancella i tre Shortcut (tieni premuto → Elimina) e il file `ela-prova.txt`
    dall'app **File**.

**Da riportare:** `~/Downloads/ela-probe.txt`, lo screenshot del passo 5 come
`~/Downloads/ela-p2-iphone.png`, e le annotazioni dei passi 5, 7, 8, 9 e 11 in
`~/Downloads/ela-p2-note.txt`.

### P3 — il codice che attraversa

Il gesto è quello vero: `ela node enroll` stampa il codice **in una tabella**, accanto alla privacy e
alla scadenza, e lo si copia a mano dal terminale. La sonda stampa la tabella con la stessa regola di
`cli/output.py` — intestazioni in maiuscolo, due spazi fra le colonne.

Controlla prima che gli Appunti universali possano funzionare: sul Mac e sull'iPhone lo stesso
account Apple, Wi-Fi e Bluetooth accesi, e **Handoff** acceso — sull'iPhone in Impostazioni →
Generali → AirPlay e Continuità (AirPlay & Continuity), sul Mac in Impostazioni di Sistema →
Generali → AirDrop e Handoff. Annota come li hai trovati; se ne hai acceso uno, dillo.

1. **Mac** — avvia la sonda: `uv run --no-project python ~/Downloads/ela_probe.py "$ip"`. Sotto la
   riga `CODE … PRIVACY  EXPIRES AT` compare il codice, 43 caratteri, poi `TRUSTED` e un'ora.
2. **Mac** — **fai doppio clic sul codice**, poi ⌘C. Guarda che cosa si è evidenziato: annota se è
   tutto il codice fino a `TRUSTED` escluso, o solo un pezzo.
3. **iPhone** — **entro un minuto**: Safari → `http://<ip>:8172/`, tocca il campo «2. Incolla qui
   il codice», tocca di nuovo → **Incolla** (Paste) → **Invia**.
4. **Mac** — guarda il terminale: la riga `form: code[…]` deve dire **`len=43 COMBACIA`**. Se dice
   `DIVERSO`, il `len=` dice quanti caratteri sono arrivati: annotalo.
5. **Mac** — se al passo 4 era `DIVERSO`: seleziona il codice **trascinando** dal primo all'ultimo
   carattere, ⌘C, e ripeti i passi 3 e 4.
6. **Ancora una volta, aspettando**: copia di nuovo il codice come al passo che ha funzionato;
   aspetta **tre minuti** con l'iPhone in mano senza copiare altro; poi ripeti il passo 3. Annota se
   «Incolla» c'era ancora, e che cosa ha registrato la riga `form:`.
7. **Mac** — `Ctrl-C`.

**Da riportare:** `~/Downloads/ela-probe.txt` (le ore della riga `--- avvio` e delle righe `form:`
dicono quanto ci è voluto), e in `~/Downloads/ela-p3-note.txt` lo stato di Handoff e che cosa ha
preso il doppio clic.

### P4 — il campanello

Con ntfy.sh, il server pubblico, senza account. Il testo di prova è quello del vocabolario proposto
— nessun contenuto — e l'argomento è un nome casuale che vive solo per questa misura.

1. **iPhone** — App Store → cerca **ntfy** (di Philipp C. Heckel) → installa → apri → **consenti le
   notifiche** quando lo chiede. Annota la versione dell'app (App Store → la sua pagina →
   «Cronologia versioni», o le impostazioni dell'app).
2. **Mac** — avvia la sonda (serve come pagina da aprire):
   `uv run --no-project python ~/Downloads/ela_probe.py "$ip"`. In **un secondo terminale**:
   ```
   cd /Users/tommasosilvi/ELA
   ip=$(tailscale ip -4 2>/dev/null || /Applications/Tailscale.app/Contents/MacOS/Tailscale ip -4)
   topic="ela-prova-$(uv run --no-project python -c 'import secrets; print(secrets.token_hex(8))')"
   echo "$topic"
   ```
3. **iPhone** — nell'app ntfy: **+** → nel campo dell'argomento (Topic) incolla quello stampato dal
   Mac — copiato con ⌘C, come in P3 —, lascia il server `ntfy.sh` → **Subscribe**.
4. **iPhone** — blocca lo schermo (tasto laterale) e appoggialo accanto al Mac.
5. **Mac** — nel secondo terminale:
   ```
   body="{\"topic\":\"$topic\",\"title\":\"ELA\",\"message\":\"WAITING APPROVAL · MEDIUM\",\"click\":\"http://$ip:8172/\"}"
   { date +%H:%M:%S; curl -s -H "Content-Type: application/json" -d "$body" https://ntfy.sh/ -w '\n%{http_code}\n'; } | tee -a ~/Downloads/ela-p4-mac.txt
   ```
   È la forma che l'adattatore userà: JSON alla radice di ntfy.sh, con l'argomento nel corpo. L'ultima
   riga deve essere `200`.
6. **iPhone** — annota **dopo quanti secondi** si è acceso lo schermo, e fai uno screenshot della
   notifica sul blocco schermo senza toccarla.
7. **iPhone** — **tocca la notifica** (e sblocca). Annota **che cosa si apre**: l'app ntfy, o Safari
   sulla pagina della sonda. Se si apre l'app ntfy, tocca il messaggio e annota se da lì si apre
   Safari.
8. **iPhone** — accendi **«Non disturbare»** (Centro di Controllo → Full immersion (Focus) → Non
   disturbare), rilancia dal Mac la seconda riga del passo 5 — `{ date …` —, e annota se la notifica
   si vede, e dove.
9. **Mac** — `Ctrl-C` nella sonda.
10. **Pulizia** — sull'iPhone **spegni «Non disturbare»**; nell'app ntfy, scorri sull'argomento →
    annulla l'iscrizione (Unsubscribe). Su ntfy.sh il messaggio resta in cache dodici ore, poi
    sparisce da sé. L'app può restare o andare: annota che cosa hai fatto.

**Da riportare:** `~/Downloads/ela-p4-mac.txt` (le due prove, una sotto l'altra),
`~/Downloads/ela-probe.txt` (dice se e quando Safari ha aperto la pagina dopo il tocco), lo
screenshot come `~/Downloads/ela-p4-iphone.png`, e le annotazioni dei passi 1 e 6–8 in
`~/Downloads/ela-p4-note.txt`.

### Il codice della sonda

Estratto dal primo comando di P1; solo libreria standard, e nessun valore scritto nel file di log.

```python
# ela_probe START — M12.5, le misure P1–P4: una sonda usa-e-getta, solo stdlib, niente di ELA.
# Uso, sul Mac:  uv run --no-project python ~/Downloads/ela_probe.py <indirizzo tailnet del Mac>
# Scrive ogni richiesta in ~/Downloads/ela-probe.txt, con l'ora di questo Mac. Non scrive MAI il
# valore di un cookie, di un header Authorization o di un campo: solo se c'era, quanto era lungo e
# con che cosa combaciava. I valori da copiare li stampa soltanto nel terminale.
import html
import ipaddress
import json
import secrets
import sys
from datetime import datetime, timedelta
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
from pathlib import Path
from urllib.parse import parse_qs

TAILNET = ipaddress.ip_network("100.64.0.0/10")
try:
    HOST = sys.argv[1]
    if ipaddress.ip_address(HOST) not in TAILNET:
        raise ValueError(HOST)
except (IndexError, ValueError):
    sys.exit("serve l'indirizzo del Mac sulla tailnet, 100.x.y.z: rilancia la riga ip=… e riprova")
PORT = 8172
LOG = Path.home() / "Downloads" / "ela-probe.txt"
COOKIES = {secrets.token_urlsafe(32): "COMBACIA-1", secrets.token_urlsafe(32): "COMBACIA-2"}
FIRST, SECOND = COOKIES
CODE = secrets.token_urlsafe(32)
BEARER = "probe." + secrets.token_urlsafe(8)


def log(line: str) -> None:
    stamp = datetime.now().astimezone().strftime("%Y-%m-%d %H:%M:%S %z")
    with LOG.open("a", encoding="utf-8") as out:
        out.write(f"{stamp} {line}\n")
    print(f"{stamp} {line}", flush=True)


def seen(value: str | None, *expected: str) -> str:
    if value is None:
        return "assente"
    if value in COOKIES and value in expected:
        return f"len={len(value)} {COOKIES[value]}"
    return f"len={len(value)} {'COMBACIA' if value in expected else 'DIVERSO'}"


def cookie_of(header: str | None) -> str | None:
    for part in (header or "").split(";"):
        name, _, value = part.strip().partition("=")
        if name == "ela_probe":
            return value
    return None


PAGE = """<!doctype html><html lang="it"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1"><title>ELA sonda</title></head>
<body style="font: 18px system-ui; margin: 24px">
<h1>ELA — sonda M12.5</h1>
<p>Cookie in questa richiesta: <b>{cookie}</b></p>
<p><a href="/set">1. Imposta il cookie</a></p>
<p><a href="/set2">1-bis. Imposta un altro cookie</a></p>
<form method="post" action="/form">
<p><label>2. Incolla qui il codice<br>
<input name="code" autocomplete="off" style="font-size:18px;width:100%"></label></p>
<p><button type="submit" style="font-size:18px">Invia</button></p>
</form>
</body></html>"""


class Probe(BaseHTTPRequestHandler):
    def _common(self) -> str:
        cookie = seen(cookie_of(self.headers.get("Cookie")), FIRST, SECOND)
        auth = self.headers.get("Authorization")
        auth_seen = seen(auth.removeprefix("Bearer ") if auth else None, BEARER)
        log(
            f"{self.command} {self.path} cookie[{cookie}] authorization[{auth_seen}] "
            f"origin[{self.headers.get('Origin', 'assente')}] "
            f"ua[{self.headers.get('User-Agent', '')}]"
        )
        return cookie

    def _send(
        self, status: int, body: bytes, kind: str, extra: dict[str, str] | None = None
    ) -> None:
        self.send_response(status)
        self.send_header("Content-Type", kind)
        for name, value in (extra or {}).items():
            self.send_header(name, value)
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def _set(self, value: str) -> None:
        attributes = "HttpOnly; SameSite=Strict; Path=/; Max-Age=34560000"
        self._send(
            303,
            b"",
            "text/plain",
            {"Location": "/", "Set-Cookie": f"ela_probe={value}; {attributes}"},
        )

    def do_GET(self) -> None:  # noqa: N802
        cookie = self._common()
        if self.path == "/set":
            self._set(FIRST)
        elif self.path == "/set2":
            self._set(SECOND)
        elif self.path == "/json":
            auth = self.headers.get("Authorization")
            answer = {
                "ok": True,
                "authorization": seen(auth.removeprefix("Bearer ") if auth else None, BEARER),
            }
            self._send(200, json.dumps(answer).encode(), "application/json")
        elif self.path == "/":
            self._send(
                200, PAGE.format(cookie=html.escape(cookie)).encode(), "text/html; charset=utf-8"
            )
        else:
            self._send(404, b"", "text/plain")

    def do_POST(self) -> None:  # noqa: N802
        self._common()
        length = int(self.headers.get("Content-Length", "0"))
        fields = parse_qs(self.rfile.read(length).decode("utf-8", "replace"))
        code = (fields.get("code") or [None])[0]
        log(f"  form: code[{seen(code.strip() if code else None, CODE)}]")
        self._send(303, b"", "text/plain", {"Location": "/"})

    def log_message(self, *args: object) -> None:
        pass


log(f"--- avvio su http://{HOST}:{PORT}/")
expires = (datetime.now().astimezone() + timedelta(minutes=10)).strftime("%Y-%m-%dT%H:%M:%S%z")
rows = [("code", "privacy", "expires at"), (CODE, "TRUSTED", expires)]
widths = [max(len(row[column]) for row in rows) for column in range(3)]
print("\nIl codice da copiare, nella tabella che stampa `ela node enroll`:\n")
print("  ".join(cell.upper().ljust(widths[n]) for n, cell in enumerate(rows[0])).rstrip())
print("  ".join(cell.ljust(widths[n]) for n, cell in enumerate(rows[1])).rstrip() + "\n")
print(f"L'header per lo Shortcut:  Bearer {BEARER}\n", flush=True)
ThreadingHTTPServer((HOST, PORT), Probe).serve_forever()
# ela_probe END
```
